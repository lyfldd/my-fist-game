# 对象池系统设计

> **来源**: 开发计划书.md §4.14 · **状态**: 📋 Design Only — 未实施
> **定位**: 性能基础设施，L4 组装层。依赖: 无（独立工具类）。
> **核心原则**: 高频创建/销毁的对象必须走池。不做过度设计，只池真正需要池的。
> **最后审查**: 2026-06-22

---

## 实施状态：DESIGN ONLY

**该系统的全部代码尚未实施。** 项目中不存在任何对象池代码，所有对象创建/销毁均使用 Unity 原生的 `Instantiate()` / `Destroy()`。

| 检查项 | 状态 |
|------|:--:|
| `Systems/Pooling/` 目录 | ❌ 不存在 |
| `ObjectPool.cs` 泛型池核心 | ❌ 不存在 |
| `PoolManager.cs` 全局管理器 | ❌ 不存在 |
| 任何 `IPoolable` / `PoolableObject` 接口或基类 | ❌ 不存在 |
| 使用 `Instantiate()` 的系统文件 | ⚠️ 45 个文件直接调用 |
| 使用 `Destroy()` 的系统文件 | ⚠️ 45 个文件直接调用 |

### 当前 Instantiate/Destroy 调用分布（按池化优先级排序）

**极高优先级（每帧/高频调用）**：

| 文件 | 调用场景 | 池化收益 |
|------|------|:--:|
| `Weapon/WeaponHolder.cs` | 子弹/投射物发射 | ⭐⭐⭐⭐⭐ |
| `VFX/MuzzleFlashSystem.cs` | 枪口火花特效 | ⭐⭐⭐⭐⭐ |
| `Combat/DamageableZombie.cs` | 血液/击中特效 | ⭐⭐⭐⭐⭐ |

**高优先级（中频调用）**：

| 文件 | 调用场景 | 池化收益 |
|------|------|:--:|
| `Zombie/ZombieSpawner.cs` | 僵尸 Instantiate | ⭐⭐⭐ |
| `ChunkManager.cs:458` | 僵尸 Destroy(gameObject) | ⭐⭐⭐ |
| `WorldContainer/WorldItem.cs` | 地面掉落物品创建/销毁 | ⭐⭐ |
| `WorldContainer/GroundItemInteract.cs` | 物品拾取销毁 | ⭐⭐ |
| `Audio/SoundEmitter.cs` | 临时音效发射器 | ⭐⭐ |
| `Audio/DecibelSystem.cs` | 音效发射器管理 | ⭐⭐ |

**中等优先级**：

| 文件 | 调用场景 | 池化收益 |
|------|------|:--:|
| `Building/GhostPreview.cs` | 建造预览 Instantiate/Destroy | ⭐⭐ |
| `Interaction/PlayerInteraction.cs` | 交互提示 | ⭐ |
| `Player/PlayerController.cs` | 玩家相关对象 | ⭐ |
| `Threat/ThreatSystem.cs` | 威胁指示器 | ⭐ |

**低优先级（不池化 — UI 面板/单例/SO）**：

| 文件 | 说明 |
|------|------|
| `Crafting/*UI.cs`, `BuildMenuUI.cs`, `ContainerWindowUI.cs` 等 | UI 面板 — 按设计原则不池化大面板 |
| `SaveLoad/SaveLoadManager.cs`, `SaveLoad/WorldItemManager.cs` | 存档系统 — Instantiate 用于恢复对象，频率极低 |
| `WorldGen/RefreshHub.cs`, `WorldContainer/ContainerRegistry.cs` | 管理类 — 仅初始化时调用，不池化 |

---

## 为什么需要对象池

```
当前问题:
- 僵尸生成 = Instantiate(僵尸prefab)    ← GC 分配
- 僵尸死亡 = Destroy(gameObject)        ← GC 释放
- 子弹发射 = Instantiate + Destroy      ← 高频 GC
- 地面物品 = Instantiate + Destroy      ← 大量 GC
- 粒子特效 = Instantiate + Destroy      ← 极高频 GC

结果: 帧率周期性卡顿（GC.Collect），大量对象 → 内存碎片。
```

**池化收益估算**:

| 对象类型 | 频率 | 生命周期 | 池化收益 |
|------|:--:|:--:|:--:|
| 僵尸 | 中 (10~50/min) | 长 (分钟级) | ⭐⭐⭐ 高 — 减少 GC 尖刺 |
| 子弹/投射物 | 高 (100~500/min) | 短 (秒级) | ⭐⭐⭐⭐⭐ 极高 — 消除每帧分配 |
| 地面物品 | 中 (掉落频率) | 长 (直到拾取) | ⭐⭐ 中 |
| 粒子特效 | 极高 (击中/血/爆炸) | 极短 (<1s) | ⭐⭐⭐⭐⭐ 极高 |
| 音效发射器 | 中 | 短 (秒级) | ⭐⭐ 中 |
| 伤害数字 UI | 高 | 极短 (<1s) | ⭐⭐⭐ 高 |

---

## 架构设计

**核心组件（2 新建文件）**:

```
Systems/Pooling/
├── ObjectPool.cs          # 泛型对象池核心
└── PoolManager.cs         # 全局池管理器（单例 + 预热 + 统计）
```

**ObjectPool\<T\> 设计**:

```csharp
/// <summary>
/// 泛型对象池。T 必须是 Component。
/// 线程不安全，仅主线程使用。
/// </summary>
public class ObjectPool<T> where T : Component
{
    // === 配置 ===
    public T Prefab { get; }           // 池化的预制体
    public int MaxSize { get; }        // 池子上限（超出后直接 Destroy）
    public float ShrinkInterval { get; } // 缩容检查间隔（秒）
    
    // === 运行时状态 ===
    public int ActiveCount { get; }    // 当前活跃对象数
    public int InactiveCount { get; }  // 池中待命对象数
    public int PeakActive { get; }     // 历史最高活跃数
    public int TotalSpawned { get; }   // 累计 Get 次数
    
    // === 核心 API ===
    T Get(Vector3 position, Quaternion rotation);    // 从池取/实例化
    T Get(Vector3 position);                         // 简化版（无旋转）
    void Release(T obj);                             // 归还池中
    void Prewarm(int count);                         // 预热
    void Shrink(int targetCount);                    // 缩容到目标数
    void Clear();                                    // 清空池（场景切换用）
}
```

**实现要点**:

```
Get:
  1. 如果池中有闲置对象 → 取最后一个（Stack.Pop，O(1)），激活 + 设位置
  2. 如果池空 + ActiveCount < MaxSize → Instantiate 新对象
  3. 如果池空 + ActiveCount >= MaxSize → 找最早活跃对象强制回收再分配（可选策略）
  4. 设置 GameObject.SetActive(true) + 重置 Transform

Release:
  1. 对象入栈（Stack.Push）
  2. 设置 GameObject.SetActive(false)
  3. 可选：将对象挂到池子根节点下（Scene 整洁）
  4. 如果 InactiveCount > 缩容阈值 → 标记缩容

缩容:
  1. 定时器触发（ShrinkInterval）
  2. 销毁超出 targetCount 的多余对象（Destroy）
  3. 只在池闲置对象数 > MaxSize 的 50% 时触发（避免频繁缩容）
```

**PoolManager 设计**:

```csharp
/// <summary>
/// 全局池管理器。单例，负责所有对象池的注册/统计/场景切换清理。
/// </summary>
public class PoolManager : MonoBehaviour
{
    // === 全局 API ===
    public static PoolManager Instance { get; }
    
    // 获取或创建池
    public ObjectPool<T> GetPool<T>(T prefab, int maxSize = 50) where T : Component;
    
    // 快捷方法（不用每次传 prefab）
    public T Spawn<T>(T prefab, Vector3 pos, Quaternion rot) where T : Component;
    public void Despawn<T>(T obj) where T : Component;
    
    // 预热
    public void Prewarm<T>(T prefab, int count) where T : Component;
    
    // 场景清理
    public void ClearAll();
    
    // 调试
    public string GetStats();    // 所有池的统计信息
}
```

---

## 池化接入清单

| 对象 | 预制体/模板 | 池大小 | 接入位置 | 改造内容 |
|------|------|:--:|------|------|
| 僵尸 | ZombieController prefab | 200 | ZombieSpawner | Spawn → pool.Get()，死亡 → pool.Release() |
| 子弹 | Bullet prefab | 100 | WeaponShooting | 每发子弹从池取，命中/超距 → 归还 |
| 地面物品 | WorldItem prefab | 200 | WorldItem 掉落逻辑 | Instantiate → Get，拾取 → Release |
| 击中火花 | HitSpark VFX | 30 | WeaponShooting/PlayerCombat | 击中时生成，播完自动归还 |
| 血液喷溅 | BloodSplatter VFX | 20 | DamageableZombie | 受伤时生成 |
| 爆炸效果 | Explosion VFX | 10 | 爆炸逻辑 | 爆炸时生成 |
| 伤害数字 | DamageNumber UI | 30 | IDamageable | 浮动数字，上升淡出后归还 |
| 音效发射器 | SoundEmitter | 20 | DecibelSystem | 临时音源，播完归还 |
| AI无人机 | Drone prefab | 10 | AIDroneBay | 待无人机系统实施时接入 |

---

## 实施步骤

| 步骤 | 内容 | 新建文件 | 改造文件 | 估时 |
|:--:|------|:--:|:--:|:--:|
| 1 | ObjectPool.cs — 泛型池核心（Get/Release/Prewarm/Shrink/Clear） | 1 | 0 | 0.5 天 |
| 2 | PoolManager.cs — 全局管理器（单例 + 注册表 + 统计 + 场景清理） | 1 | 0 | 0.5 天 |
| 3 | VFX 接入 — 击中火花 + 血液喷溅 + 爆炸效果 | 0 | 3 | 0.25 天 |
| 4 | 僵尸接入 — ZombieSpawner 改为 pool.Get()，死亡改为 pool.Release() | 0 | 2 | 0.5 天 |
| 5 | 子弹接入 — WeaponShooting 改为池化子弹 | 0 | 1 | 0.25 天 |
| 6 | 地面物品接入 — WorldItem 掉落/拾取池化 | 0 | 2 | 0.25 天 |
| 7 | UI 伤害数字 — DamageNumber 池化 | 1 | 1 | 0.25 天 |
| 8 | 调试面板 — F2 增加池统计面板 | 0 | 1 | 0.25 天 |
| 9 | 编辑器工具 — 一键配置池子参数 | 1 | 0 | 0.25 天 |
| | **合计** | **4 新建** | **~10 改造** | **~3 天** |

---

## 池统计调试面板（F2 新增 Tab）

```
┌─ 对象池统计 ──────────────────────┐
│                                    │
│ 类型        活跃   闲置   峰值     │
│ ──────────────────────────────     │
│ 僵尸        23    177    45        │
│ 子弹         8     92    25        │
│ 地面物品    45    155    78        │
│ 击中火花     3     27    12        │
│ 血液喷溅     1     19     5        │
│ 伤害数字     5     25    15        │
│ 音效发射器   2     18     8        │
│                                    │
│ 总计活跃: 87  总计闲置: 513        │
│ [全部缩容] [清空全部池]            │
└────────────────────────────────────┘
```

---

## 注意事项

| 项 | 说明 |
|------|------|
| **归还时重置** | Release 时必须重置对象状态（HP/动画/速度/颜色），否则下次 Get 拿到"脏"对象 |
| **OnEnable/OnDisable** | 依赖 Awake/Start 做初始化的组件，改为依赖 OnEnable（每次从池取出触发） |
| **池上限** | 超过 MaxSize 且池中无闲置 → 扩容（不卡），而非报错。MaxSize 是"缩容目标"不是"硬上限" |
| **场景切换** | PoolManager 必须是 DontDestroyOnLoad，场景切换时 ClearAll() |
| **预制体匹配** | 每个池绑定特定预制体，不同僵尸类型 = 不同池 |
| **不池化 UI 面板** | 只池化频繁创建/销毁的小 UI 元素（伤害数字），不池化大面板（背包/合成/HUD） |

---

## 实施注意事项（基于代码审查）

1. **ZombieSpawner 改造要点**: `SpawnZombie()` 方法（ZombieSpawner.cs:259）当前使用 `Instantiate()`，需改为 `pool.Get()`。同时 `DamageableZombie` 的死亡逻辑需在销毁前调用 `pool.Release()`。
2. **ChunkManager 僵尸清理**: `DestroyZombiesInChunk()`（ChunkManager.cs:446）使用 `Destroy()`，改为 `pool.Release()` 后需确保僵尸状态（Agent/动画/HP）在 Release 时被重置。
3. **WeaponHolder 子弹**: 需确认子弹的飞行逻辑在命中/超距后有明确的"归还"入口点。
4. **VFX 自动归还**: 粒子特效播完后需自动归还。可在 VFX 上挂 `OnParticleSystemStopped` 回调，或使用协程等待 duration 后归还。
5. **WorldItem 存档兼容**: 地面物品涉及存档系统（WorldItemManager.cs、SaveLoadManager.cs），池化后需确保 `PersistentGUID` 的引用在 Get/Release 之间正确维护。

---

*本文档是 `开发计划书.md` §4.14 对象池系统的详细设计拆分。*
