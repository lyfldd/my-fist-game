# 2026-06-22

## 存档系统设计文档
- 基于 §4.13 存档系统设计 + 项目审查修正，生成独立设计文档 `开发计划/存档系统设计文档.md`
- 审查发现 7 处问题并全部修正写入文档：
  1. PersistentGUID 加载覆盖时机 → 改为两阶段初始化 (Awake生成 / Start或显式Initialize注册)
  2. 加载 Phase 4→5 同步缺失 → 明确 WorldGenCompleted 和 WorldEntitiesRestored 两个同步点
  3. 天气马尔可夫链状态不完整 → 补 recentWeatherHistory 字段
  4. AIBot 字段规范 → 字段完整列出
  5. VehicleSaveData 一致性 → 补 position/rotation
  6. containerId 生成规则 → 说明世界生成时分配、跨会话稳定
  7. 世界生成完成信号 → WorldGenCompleted 事件
- 文档包含：项目背景、技术选型论证、PersistentGUID 陷阱与方案、完整数据模型、加载流程同步点、各系统接入兼容性

## 存档系统设计文档 v3.0 第二轮修正
- 基于用户 9 项审查意见全面修正：
  1. **装备槽状态丢失** → equippedItems 改为 EquippedItemSaveData(containerId+slotIndex+durability)，解决同名不同耐久物品歧义
  2. **地面物品去重脆弱** → WorldItemSaveData 加 instanceId（递增int），放弃浮点坐标去重
  3. **线程安全** → 主线程采集后深拷贝(JsonUtility序列化往返)，切断引用共享再交后台线程
  4. **截图阻塞** → 改为 AsyncGPUReadback + RenderTexture.GetTemporary 异步，主线程不等待
  5. **GC压力** → 预分配列表容量 + 复用SaveData根对象 + struct替代class建议
  6. **原子备份** → 原"Copy→Move"改为全程File.Move(rename)，Copy非原子会出半截备份
  7. **存档槽覆盖** → 从"自动覆盖最老"改为交互式确认UI，玩家手动选覆盖目标
  8. **僵尸洗存档漏洞** → 从"不保存僵尸"改为保存30m半径内僵尸（位置/血量/状态/目标），睡觉前检查附近无僵尸
  9. **加载逻辑冻结** → 新增Phase 0全局冻结(GameState.Loading + timeScale=0 + InputLockManager)，Phase 7解冻
- 新增：ZombieSaveData、GameState枚举、GameStateChanged事件、防洗档机制章节、深度拷贝代码示例、异步截图代码示例
- 708行 → 917行 (+209行)

## 开发计划书结构重组
- 将 `开发计划书.md` 从单一巨文档拆分为目录+链接模式
- 3412行 → 1615行（压缩53%）
- 拆分出5个独立设计文档到 `开发计划/` 目录：
  - `电力系统.md` — §4.3（273行）
  - `技能与工业系统重构.md` — §4.5（200行）
  - `区域系统.md` — §4.4（159行）
  - `对象池系统.md` — §4.14（193行）
  - `UI系统重构计划.md` — UI重构章节（72行）
- §4.7 AI机器人、§4.7.1 AI驾驶、§4.8 无人机 压缩为摘要+已有设计文档链接
- §4.13 存档 压缩为摘要+存档系统设计文档链接
- 使用 Python regex 批量压缩，临时脚本 `_compress.py` 已清理
