# 2026-06-24

## 用户偏好确认
- 审查任务只指出问题不修复（已写入 MEMORY.md）

## 耐久系统 代码 vs 计划 完成度审查（第1轮·已过时）
- 12 项缺失中大部分是计划内延期，阿铁确认：
  - RepairUI/RepairBench/ItemRepairedEvent → 耐久 Phase 4
  - 全局耐久UI/TopLeftHUD车辆血条/冒烟VFX → 耐久全局UI.md
  - CreateCraftingItems 批量 itemMaterial → 编辑器增强

## 耐久系统 代码 vs 计划 完成度审查（第2轮·排除计划内延期后）
- 已确认修复：DurabilitySystem已挂载(GameBootstrap:278) / maxRepairs已存在(ItemData:33) / TODO注释已更新 / 左臂已加ConsumeWeaponDurability(AIBotCombat:785) / ItemBrokenEvent已有订阅者(WeaponHolder+SurvivalSystem)

### P0 致命bug：ItemBrokenEvent 损坏处理链断裂
- `DurabilitySystem.cs:72` 发布时硬编码 `EquipSlot.None`
- `WeaponHolder.cs:128` `if (evt.EquipSlot == EquipSlot.None) return;` → 永远跳过
- `SurvivalSystem.cs:108` `if (evt.EquipSlot != EquipSlot.None)` → 永远不进入
- 根因：Inventory 缺 instanceId→EquipSlot 反向查询方法（_equippedInstanceIds 是正向 Dictionary<EquipSlot,int>）
- 结果：武器损坏不卸下、护甲损坏不重算

### P1 文档矛盾：耐久全局UI.md 标注前置K"已完成"
- 耐久全局UI.md:18 写"✅ 前置K 已实现"
- 实际 InventoryUI.cs/QuickItemBar.cs 都无耐久条代码
- 前置K应归入全局UI阶段(未完成)

### P2 潜在问题：SurvivalSystem.OnItemBroken 重算护甲未考虑耐久衰减
- SurvivalSystem.cs:115 `total += kv.Value.armorValue` 直接用原始值
- 应调用 `inv.GetTotalArmor()`（已考虑耐久衰减）
- 仅在 P0 bug 修复后才会暴露

## 耐久全局UI.md 修订（按指导意见落实）
- 6 项指导意见全部写入文档：
  1. 前置K 状态改为 ⬜ 归入本阶段（原错误标✅）
  2. IDurabilityTarget 拆出 DurabilityDisplayType 枚举（Health/Durability/Wear 三类）
  3. 准星检测改为复用 PlayerInteraction.CurrentTarget（避免重复射线）
  4. 地面物品加 hasDurability 过滤（Ratio 1.0 自动隐藏）
  5. 三种实体刷新机制分开（AIBot 轮询/设备事件/车辆事件）
  6. P0 bug 列为执行前置条件
- 版本升至 v1.1

## 背包系统.md 代码vs计划审查
- 核心架构完成度 ~95%
- **4处文档与代码不一致**
  1. §9"耐久系统字段已预留但未接入"已过时(已接入DurabilitySystem+耐久条)
  2. §9"快捷栏与背包联动优化(独立工作)"已过时(QuickItemBar已订阅InventoryChanged+DurabilityChangedEvent)
  3. §5.1"制作Tab显示开发中"—CraftingUI.cs已存在但背包Tab点制作仍显示开发中
  4. InventoryUI:2339快捷面板标题用maxWeight(基础30)而非EffectiveMaxWeight(含力量加成)

- **5处未完成项**(§9已列): 右键菜单/排序/对比提示/AIBotInventory独立/§9第一条过时

- **2处设计遗漏**(文档未提及)
  1. **DropItem耐久丢失漏洞** — Inventory.DropItem(:807)只设itemData+count未设wi.itemDurability/repairCount，武器砍到50%耐久丢地上拾取变满耐久
  2. **GetTotalWarmth不对称** — GetTotalArmor(:493)已按耐久比衰减，但GetTotalWarmth(:527)直接用warmthValue原始值，0%耐久衣物仍满保暖
