# 2026-06-24

## 用户偏好确认
- 审查任务只指出问题不修复（已写入 MEMORY.md）

## 耐久系统 代码 vs 计划 完成度审查（第1轮·已过时）
- 12 项缺失中大部分是计划内延期，阿铁确认：
  - RepairUI/RepairBench/ItemRepairedEvent → 耐久 Phase 4
  - 全局耐久UI/TopLeftHUD车辆血条/冒烟VFX → 耐久全局UI.md
  - CreateCraftingItems 批量 itemMaterial → 编辑器增强

## 耐久系统 代码 vs 计划 完成度审查（第2轮·排除计划内延期后）
- 已确认修复：DurabilitySystem已挂载(GameBootstrap:278) / maxRepairs已存在(ItemData:33) / TODO注释已更新 / 左臂已加ConsumeWeaponDurability(AIBotCombat:785) / ItemBrokenEvent已有订阅者(WeaponHolder+SurvivalSystem)

### P0 致命bug：ItemBrokenEvent 损坏处理链断裂
- `DurabilitySystem.cs:72` 发布时硬编码 `EquipSlot.None`
- `WeaponHolder.cs:128` `if (evt.EquipSlot == EquipSlot.None) return;` → 永远跳过
- `SurvivalSystem.cs:108` `if (evt.EquipSlot != EquipSlot.None)` → 永远不进入
- 根因：Inventory 缺 instanceId→EquipSlot 反向查询方法（_equippedInstanceIds 是正向 Dictionary<EquipSlot,int>）
- 结果：武器损坏不卸下、护甲损坏不重算

### P1 文档矛盾：耐久全局UI.md 标注前置K"已完成"
- 耐久全局UI.md:18 写"✅ 前置K 已实现"
- 实际 InventoryUI.cs/QuickItemBar.cs 都无耐久条代码
- 前置K应归入全局UI阶段(未完成)

### P2 潜在问题：SurvivalSystem.OnItemBroken 重算护甲未考虑耐久衰减
- SurvivalSystem.cs:115 `total += kv.Value.armorValue` 直接用原始值
- 应调用 `inv.GetTotalArmor()`（已考虑耐久衰减）
- 仅在 P0 bug 修复后才会暴露

## 耐久全局UI.md 修订（按指导意见落实）
- 6 项指导意见全部写入文档：
  1. 前置K 状态改为 ⬜ 归入本阶段（原错误标✅）
  2. IDurabilityTarget 拆出 DurabilityDisplayType 枚举（Health/Durability/Wear 三类）
  3. 准星检测改为复用 PlayerInteraction.CurrentTarget（避免重复射线）
  4. 地面物品加 hasDurability 过滤（Ratio 1.0 自动隐藏）
  5. 三种实体刷新机制分开（AIBot 轮询/设备事件/车辆事件）
  6. P0 bug 列为执行前置条件
- 版本升至 v1.1

## 背包系统.md 代码vs计划审查
- 核心架构完成度 ~95%
- **4处文档与代码不一致**
  1. §9"耐久系统字段已预留但未接入"已过时(已接入DurabilitySystem+耐久条)
  2. §9"快捷栏与背包联动优化(独立工作)"已过时(QuickItemBar已订阅InventoryChanged+DurabilityChangedEvent)
  3. §5.1"制作Tab显示开发中"—CraftingUI.cs已存在但背包Tab点制作仍显示开发中
  4. InventoryUI:2339快捷面板标题用maxWeight(基础30)而非EffectiveMaxWeight(含力量加成)

- **5处未完成项**(§9已列): 右键菜单/排序/对比提示/AIBotInventory独立/§9第一条过时

- **2处设计遗漏**(文档未提及)
  1. **DropItem耐久丢失漏洞** — Inventory.DropItem(:807)只设itemData+count未设wi.itemDurability/repairCount，武器砍到50%耐久丢地上拾取变满耐久
  2. **GetTotalWarmth不对称** — GetTotalArmor(:493)已按耐久比衰减，但GetTotalWarmth(:527)直接用warmthValue原始值，0%耐久衣物仍满保暖

## 背包UI升级计划.md 审查
- 结合代码逐行验证 §2 当前问题 + §4 技术方案 + §5 已验证不可行方案
- **3个根因诊断修正** + **4个方案缺陷** + **2个设计遗漏**

### P0 根因诊断修正
1. §2.2问题B"CleanCells杀边框"描述不完整 — 实际是overlay随scrollContent子节点被Destroy陪葬
2. §2.2问题C"空格子无法点击"遗漏关键点 — 空格子没有IPointerHandler组件，UGUI不路由事件
3. §5.1根因未指出ClearContainer用Destroy(非DestroyImmediate)导致帧末才销毁的时序问题

### P1 方案缺陷
1. §4.1 RefreshSelectionBorder在重建后调用，_cellRegions已清空重注册，SelectedItem可能与新布局不匹配
2. §4.2方案B _clickConsumed时序理论上是通的，需查ItemDragHandler是否所有路径都设了
3. §4.3增量刷新遗漏：ShowOverview每次重建标签栏+_containerCollapsed会丢失
4. §5.4建议方向可行但要处理重建后格子坐标变化

### P2 设计遗漏（已有bug）
1. **TryMoveToContainer(:572) new PlacedItem丢失instanceId和itemDurability** — 构造函数设instanceId=0/itemDurability=0f
2. **OnInventoryChanged→ShowOverview用Destroy非DestroyImmediate** — 同帧多次事件格子重复注册

## 背包重构计划.md 审查（第1轮）
- 结合实际代码逐文件审计：InventoryContainer / Inventory / DragDropManager / InventoryUI / QuickItemBar / AIBotInventory / ContainerRegistry / ContainerWindowUI / ProductionDevice / ProductionDeviceUI / DurabilitySystem / WeaponAttachmentHandler / ElectronicDevice / PlayerInteraction / 存档系统
- **23 个问题**：6 严重(S) + 10 中等(M) + 7 轻微(L)
- 审查结果已写入 `开发计划/背包重构计划.md` 文档头部
- 核心建议：砍掉 ContainerLayout（过度设计），GridInventory2D 直接替换 InventoryContainer；补全幽灵格管理和附件系统 API；遗漏 3 个系统（ElectronicDevice / PlayerInteraction / InventoryTest）

## 背包重构计划.md v2.0 方案审查（第2轮）
- 阿铁在文档中写了 v2.0 修正方案，对照代码逐条验证
- **20/23 项已正确修正**，新发现 **9 个问题**（2 严重 + 5 中等 + 2 轻微）
- 关键问题：
  - S7: GridInventory2D API 缺 Resize 方法（装备系统强依赖）
  - S8: CountFreeForGhost 公式写反（减应为加，会导致武器装不上）
  - M11: Resize 缩小时溢出策略未定义
  - M12: 跨容器拖拽缺原子性（先 Remove 后 Place 可能丢物品）
  - M15: DropItem 耐久丢失 bug 未提及
- 结论：v2.0 可进入实现，但 S7/S8 必须先修

## 系统接入规范.md 审查（第1轮）
- 结合实际代码逐文件审计：ItemData / WeaponShooting / PlayerCombat / SurvivalSystem / DurabilitySystem / ElectronicDevice / WeaponAttachmentHandler / Inventory / ProductionDevice / VehicleController / AIBotCombat / ItemGraphBuilder / BuildModeController
- **22 个问题**：9 严重(S) + 8 中等(M) + 5 轻微(L)
- 审查结果已写入 `开发计划/系统接入规范.md` 文档头部
- 核心发现：
  - S1: §3 引用完整性检验完全没实现（代码 0 匹配）
  - S2: ElectronicDevice 不读 ItemData.electronicDeviceType，用 itemName 硬编码查找
  - S3: healAmount 字段完全没用，foodType 不是触发条件
  - S4: WeaponShooting 不检查 category==Weapon，只看 isFirearm
  - S5: AIBotWeaponMapping.asset 不存在，全是硬编码 const 字符串
  - S6: 附件"或"逻辑会误识别武器为附件
  - S7: VehicleController 不用 fuelValue，用硬编码 fuelItemName
  - 死字段：healAmount / fuelValue / electronicDeviceType
- 结论：规范描述的是理想状态，不是当前代码状态；5 个严重问题会导致"按文档填字段但功能不生效"

## 物品系统关联升级.md 审查（第1轮）
- 结合实际代码逐文件审计：VehicleController / ElectronicDevice / AIBotCombat / AIBot / AIBotUI / AIBotInventory / SurvivalSystem / DurabilitySystem / Inventory / ItemGraphBuilder / WeaponShooting / ProductionDevice / BuildModeController / PlayerInteraction / RecipeCatalog
- **18 个问题**：5 严重(S) + 8 中等(M) + 5 轻微(L)
- 审查结果已写入 `开发计划/物品系统关联升级.md` 文档头部
- 核心发现：
  - S1: 遗漏 AIBotUI（7 处硬编码：电池组/浓缩铀/高级零件/铁锭/聚变核心小/大）
  - S2: 遗漏 AIBot.cs（IsSmall/IsLarge 硬编码聚变核心名）
  - S3: VehicleController 方案错误（"取第一个 fuelValue>0"会有多燃料冲突）
  - S4: ElectronicDevice 方案不完整（遗漏 ReplaceBattery + 多设备冲突未解决）
  - S5: AIBotCombat 方案不完整（只删 const 不删 12+ 个 Inspector 字段：pistolDamage/rifleDamage 等）
  - M6: WeaponShooting 用 ammoItemName 字符串（应改为 ItemData.ammoItemData 引用）
  - M8: AIBotInventory 按 itemName 查物品
  - 建议：ItemData 加 5 个新字段（energyValue/repairValue/fusionCoreSize/acceptedFuels[]/ammoItemData）
  - 估时 2.5天 → 5-6天

## 三份计划完成度核查（系统接入规范 / 武器与装备重设计划 / 物品系统关联升级）
- 核查结果写入 `开发计划/三份计划完成度核查.md`
- 阿铁今天做了大量实现工作（比上午审查时进步很多）：
  - ItemGraphBuilder 引用完整性检验 ✅ 已实现（ValidateReferences）
  - AIBotWeaponMapping.cs ✅ 建了（.asset 未建）
  - AIBotCombat 改读映射表 ✅（MapDamage/MapCooldown 优先 mapping）
  - VehicleController acceptedFuels ✅（按 fuelValue 排序）
  - ElectronicDevice 改读 electronicDeviceType + instanceId 绑定 ✅（ReplaceBattery 逻辑错误）
  - SurvivalSystem healAmount ✅ 已激活（纯 foodType 仍不触发）
  - AIBotUI 7处硬编码 ✅ 全改（energyValue/repairValue/fusionCoreSize）
  - AIBot.cs 聚变核心 ✅ 全改（coreSize 字段）
  - ItemData 加 4 新字段 ✅（energyValue/repairValue/fusionCoreSize/ammoItemData）
  - 武器重设代码改动 ✅ 全改（6处硬编码）
  - 新武器弹药 .asset ✅ 全建（8火器+5弹药+组件+特殊武器）
- 遗留问题：
  - 🔴 旧 RecipeData 未删（引用已删物品→null→ItemGraph 断裂）
  - 🔴 ElectronicDevice.ReplaceBattery 逻辑错误（Flashlight && "电池组" 永不匹配）
  - 🔴 AIBotWeaponMapping.asset 未建（运行时走 fallback 硬编码）
  - 🔴 DurabilitySystem 仍未加 maxDurability>0 检查
  - 🟡 8个装备.asset缺失（鞋子全缺）+ 3个材料缺失（尼龙/凯夫拉/陶瓷板）
  - 🟡 AIBotInventory/PlayerInteraction 仍直接访问 placedItems
  - 🟡 WeaponShooting ammoItemData 只改了一半
  - 🟡 Inventory.ApplyEquip:180 无兜底
- 完成度：系统接入规范80% / 武器重设70% / 物品关联升级60%
