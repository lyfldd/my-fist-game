# 2026-05-09 开发记录

## 多容器系统重构 ✅
- 创建 `InventoryContainer.cs` — 独立网格+物品+增删方法
- 重写 `Inventory.cs` — 管理 5 个容器（上衣/裤子/腰带/胸挂/背包）
- `EquippedItems` 结构体组织装备槽
- `OnValidate` 自动校验装备位（防止装错槽）
- `UnequipSlot` 支持，连带清空容器内容

## 装备体系扩展 ✅
- `EquipSlot` 增加 `Tops` 枚举
- `CreateDefaultItems.cs` 每个装备槽 2 件（民用+军用）
- 装备分类子文件夹：Tops/Pants/Belts/Vests/Backpacks
- 创建 `SetupOverviewPanel.cs` 一键生成总览面板

## UI 双面板系统 ✅
- **Tab** = 总览面板（所有容器缩略网格，垂直排列）
- **V** = 快捷面板（单容器全尺寸，循环切换）
- **4/5/6/7/8** = 直开上衣/胸挂/腰带/裤子/背包
- **ESC** = 关闭所有面板
- 总览打开时屏蔽所有其他快捷键
- 动态记录 `_lastQuickSlot`，脱装备后自动兜底检索
- `ExtensionArea` 预留扩展区

## Bug 修复
- LayoutGroup 冲突（VLG vs GLG 互斥）
- `_isOverviewMode` 状态未重置导致 V 打开异常
- _panel 改为 public 手动拖绑，不再依赖 GetChild(0)
- 两面板彻底独立，各自管理自己的 LayoutGroup

## 用户要求
- 代码要有可扩展性、解耦
- 要有防呆设计
- **下次优先加固防呆**（脱下装备先行检查、容器尺寸变更时物品溢出处理）
