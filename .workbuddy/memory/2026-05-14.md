# Daily Log 2026-05-14

## 总面板 UI 全面重构（骨架式 + 武器槽）

### 三区全部改为骨架式设计
- **Zone 1（人物+装备槽）**: 
  - 重写 DrawEquipSlots，文字在上、格子在下
  - 新增4个武器槽：主武器、副武器、小刀(腰带解锁)、手枪(腰带解锁)
  - 小刀/手枪位依赖腰带装备状态，未装备时灰色锁定
  - 新增 EquipSlot: RightHand/LeftHand/KnifeBelt/SidearmBelt
  - 间距从 8px 减到 4px
- **Zone 2（装备容器+预留C5）**:
  - 重写为 DrawEquipArea（骨架+填充两遍式）
  - 预画所有空网格骨架（暗色），再覆盖物品填充
  - 标签加暗色背景底色，不再飘浮
- **Zone 3（背包+属性）**: 同用 DrawEquipArea

### 修复
- 修复局部函数闭包导致的 CS8801 花括号不平衡错误（152个错误）
- 修复双份 C5 占位区代码

### 武器模型
- 长刀模型已导入（.fbx格式），贴图重命名为标准命名
- 安装工具脚本 SetupWeaponModel / FixWeaponMaterials
- 模型展示暂停（先做系统，后展示）

### 技术债务
- 旧 BuildContainerGrid 方法保留不删，已不再调用
- 旧 DrawEquipSlotCell 已删除
- `using` 需补充 EquipSlot 新值

## 武器系统完整实现（Phase 1~6 全完成）

### Phase 1: 数据层
- ItemData.cs +武器属性段（isFirearm/weaponDamage/fireRate/baseSpread/maxSpread/spreadRecovery/spreadPerShot/magazineSize/range）
- GameEvents.cs +3 个武器事件（WeaponEquippedEvent/WeaponUnequippedEvent/WeaponFiredEvent）
- 4 个 .asset 装备位修正（WoodenStick/LongSword/AK47→RightHand, DesertEagle→SidearmBelt）
- CreateDefaultItems.cs 新增 [Tools/Update Weapon Data] 菜单

### Phase 2: 装备逻辑
- Inventory.cs: EquipItem/UnequipSlot 武器槽分支 + IsWeaponSlot() + 腰带 ghost 管理
- PlacedItem.cs: 新增 isGhost/ghostSourceSlot + Ghost() 工厂方法
- 腰带拆除级联：卸下 Belt → 自动卸下 KnifeBelt/SidearmBelt

### Phase 3: 模型绑定
- WeaponHolder.cs: 订阅装备事件、RightHandPoint 挂载、模型 Spawn/Destroy、PlayerCombat 自动切换

### Phase 4: 瞄准系统
- WeaponAiming.cs: 鼠标→地面平面求交 + 辅助瞄准 2m 吸附 + 僵尸胸部锁定
- CameraFollow.cs: angle 45°→60°

### Phase 5: 射击系统
- WeaponShooting.cs: Rodrigues 旋转锥形散射 + 射线命中 + IDamageable.TakeDamage + 散射恢复

### Phase 6: 整合
- WeaponHolder.UpdateCombatState() 管理 PlayerCombat 启用/禁用
- GetComponent("PlayerCombat") 字符串查找避免命名空间耦合

### Unity 端待办
1. 运行 Tools/Update Weapon Data 设置武器属性
2. AK47.fbx / DesertEagle.fbx → Prefab → 设到 worldPrefab
3. 玩家 GameObject 挂载 WeaponHolder + WeaponAiming + WeaponShooting
4. WeaponAiming 设 zombieLayer 到 Zombie 层

---

## 修复武器模型未固定在右手的问题 (22:09)

### 诊断
- **场景检查**: 玩家 GameObject (fileID=570845283) 上只有 13 个组件，缺少 WeaponHolder/WeaponAiming/WeaponShooting
- **资产检查**: AK47.asset/DesertEagle.asset 的 worldPrefab 为 {fileID: 0}，只有 LongSword.asset 已设置
- **代码问题**: RightHandPoint 是玩家根节点的固定偏移子物体，Capsule 无骨架，武器只是浮在固定相对位置

### 修复
- WeaponHolder.cs: 
  - 新增 FindHandBone() 骨骼搜索（为将来有角色模型做准备）
  - 新增 _usingBone 标记，Capsule 模式用 LateUpdate 每帧更新 RightHandPoint
  - capsuleHandOffset 从 (0.35,1.1,0.3) 调整为 (0.45,1.45,0.25)（更接近右手位置）
  - 新增 HandWorldPos 属性供 WeaponAiming/WeaponShooting 读取
  - Update 改为 LateUpdate
- WeaponAiming.cs: muzzlePos 改用 _holder.HandWorldPos，不再手动 Find("RightHandPoint")
- WeaponShooting.cs: GetMuzzlePosition 改用 _holder.HandWorldPos

### 仍需 Unity Editor 操作
1. **在玩家上添加组件**: WeaponHolder + WeaponAiming + WeaponShooting
2. **设置 worldPrefab**: AK47.asset→AK47.prefab, DesertEagle.asset→DesertEagle.prefab, WoodenStick.asset→(暂无模型)
3. **调整偏移**: 进 Play 模式后，在 Inspector 调 capsuleHandOffset 直到武器位置自然

---

## 战斗系统 Phase1 完成 + Git 提交 (22:33)

- 战斗系统标记为 ✅ 完成
- Git commit: `战斗系统(阶段2-L3): Phase1完成 — 武器数据层+装备逻辑+模型绑定+瞄准+射击+UI骨架式重构` (31 files, +2235/-61)
- 下一步: 建造系统框架设计
