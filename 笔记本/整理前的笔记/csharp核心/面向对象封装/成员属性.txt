成员属性就是用来保护成员变量为成员属性的获取和赋值添加逻辑处理解决3p局限问题public——内外访问private——内部访问protected——内部和子类访问这些都有局限性，比如说我们在用publi时我们希望我们不要被修改属性可以让成员在外部只可以被获取，不能被修改，或者是是能被修改不能被获取成员属性的基本语法访问修饰符 属性类型 属性名{get{}set{}}class Person{    private int age;    private string name;    private int money;    public string Name    {        get （获取私有的值）        {	这里加规则            return name;        }	这里加规则        set（修改里面私有的值）（value用来表示外部的值）        {        }    }}就相当于我们可以通过get来获取内部的值还可以在返回之前添加一些规则我们还可以加密        public int Money        {            get            {                return money-5;            }            set            {                money= value+5;（这里就是简单的加密）（就是说我们访问内存的时候												看到的值是10，而不是5）            }        }get和set前面可以加一点访问修饰符1.默认不加的话，会使用属性声明时的访问权限2.加的访问修饰符要低于属性的访问修饰符3.不能让get和set的访问修饰符权限都低于属性的权限        public int Money        {            private get            {                return money-5;            }            set            {                money= value+5;            }        }这里就相当于我们只能修改不能获得            p.Money = 5;            Console.WriteLine(p.Money);然后系统就会报错        public int Money        {            get            {                return money-5;            }            private set            {                money= value+5;            }        }这样子就可以达到只能得到不能改变get和set可以只有一个这个时候我们就是能有一个功能就是只有get那就只能得到这个值自动属性就是我们希望只读不改，然后我们又不做什么规则那我们就用自动属性        public float Height        {            get;            set;        }