# 📋 三份计划完成度核查

> **日期**: 2026-06-24 · **核查人**: 六月份
> **范围**: 系统接入规范.md / 武器与装备重设计划.md / 物品系统关联升级.md
> **方法**: 逐项对照实际代码 + .asset 文件验证

---

## 一、系统接入规范.md（5 项）

| # | 计划项 | 状态 | 证据 |
|:--:|------|:--:|------|
| 1 | ItemGraphBuilder 加引用完整性检验 | ✅ | `ItemGraphBuilder.cs:438-513` ValidateReferences()，含配方/武器弹药/建造材料/生产设备 4 种检验，支持 ammoItemData 优先 |
| 2 | 新建 AIBotWeaponMapping.asset + 编辑器 | ⚠️ | `AIBotWeaponMapping.cs` 已建（含 Entry/GetRight/GetLeft），**.asset 文件未建**（Glob 无结果）→ 运行时 _weaponMapping 为 null，走 fallback |
| 3 | AIBotCombat 改读映射表 | ✅ | `AIBotCombat.cs:105` `_weaponMapping` 字段 + `MapDamage/MapCooldown:108-109` 优先读 mapping，fallback Inspector 字段 |
| 4 | Tools → 物品图谱 → 构建并验证菜单 | ✅ | Build() 末尾自动调 ValidateReferences() |
| 5 | 全面测试 | ⬜ | 无法从代码判断 |

**完成度: 3/4 已做 + 1 部分做（.asset 未建）**

---

## 二、武器与装备重设计划.md（10 步）

| # | 计划项 | 状态 | 详情 |
|:--:|------|:--:|------|
| 1 | 改代码硬编码（6 处） | ✅ | AIBotCombat:100-103 弹药常量→新名 / :645-647 武器名→M1911等 / :680-682 反向映射 / AIBotPilot:262-265 / PreconfigureUI:309 / AIBotUI:614,925-928 全改 |
| 2 | 删旧武器弹药 .asset | ✅ | Glob 查"手枪/步枪/霰弹枪/沙漠之鹰/猎枪/重狙/手枪子弹/步枪子弹/霰弹" → 0 结果 |
| 3 | 删旧装备 .asset | ✅ | LeatherArmor/IronArmor/BulletproofVest/WoodShield/IronShield 已删（Shield.prefab 3D模型保留） |
| 4 | 建新武器弹药 .asset | ✅ | 8 火器全建（M1911/M9/AK47/M16A1/SKS/SVD/Uzi/Remington870）+ 5 弹药全建（Ammo_9mm/45ACP/762mm/556mm/12Gauge）+ 弹药组件4个（BulletCasing/BulletHead/Primer/BlackPowder）+ 特殊武器4个（Crossbow/CompoundBow/GrenadeLauncher/Railgun）|
| 5 | 建新装备 .asset | ⚠️ | **部分建**（见下表） |
| 6 | 建新材料 .asset | ⚠️ | **1/4**：光学透镜✅ / 尼龙❌ / 凯夫拉❌ / 陶瓷板❌ |
| 7 | 重建 RecipeData | ⚠️ | 新配方建了（弹药装填机_9mm手枪弹 等），**旧配方未删**（武器组装台_手枪/步枪/霰弹枪/重狙 + 简易工作台/手枪子弹/步枪子弹/霰弹 + 中级工作台/弩箭 仍存在，引用已删的旧物品 → resultItem 为 null → 引用完整性检验会报错） |
| 8 | 重建 ItemGraph | ⬜ | 需手动运行 Tools 菜单，无法从代码判断 |
| 9 | 更新编辑器脚本 | ⬜ | 待查 CreateDefaultItems/CreateCraftingItems |
| 10 | 测试 | ⬜ | 无法从代码判断 |

### 装备 .asset 完成度明细

| 部位 | 计划数 | 已建 | 缺失 |
|------|:--:|:--:|------|
| 头部 | 4 | 4 ✅ | — |
| 上衣 | 4 | 2 | 皮夹克、防弹衣 |
| 裤子 | 3 | 2 | 工装裤 |
| 背心 | 3 | 2 | 插板背心 |
| 腰带 | 3 | 2 | 工具腰带（注：ToolBelt.asset 存在但可能是 EquipSlot.Belt 装备，需确认） |
| 背包 | 2 | 2 ✅ | — |
| 鞋子 | 3 | 0 ❌ | 运动鞋、工装靴、军靴（Glob Shoes/Boots 无结果） |
| **合计** | 22 | 14 | **8 个缺失** |

**完成度: 代码改动100% / .asset 约70% / 旧配方未清理**

---

## 三、物品系统关联升级.md（7 项）

| # | 计划项 | 状态 | 证据 |
|:--:|------|:--:|------|
| 1 | VehicleController 燃料硬编码 → fuelValue | ✅ | `VehicleController.cs:289-296` acceptedFuels 优先（按 fuelValue 排序取最高）+ fuelItemName fallback |
| 2 | ElectronicDevice 硬编码 → electronicDeviceType | ⚠️ | GetDurabilityRatio ✅ 改读 electronicDeviceType + _boundInstanceId 精确查找（解决多设备冲突）；**ReplaceBattery:110-111 逻辑错误**（要求 `electronicDeviceType==Flashlight && itemName=="电池组"`，永远不会匹配） |
| 3 | AIBotCombat 硬编码 → AIBotWeaponMapping | ✅ | _weaponMapping 字段 + MapDamage/MapCooldown 优先读 mapping（但 .asset 未建，实际走 fallback） |
| 4 | SurvivalSystem healAmount/foodType | ⚠️ | healAmount ✅ 已激活（:267-272 独立处理回血）；**纯 foodType 无 itemEffects 仍不触发**（:274 `else if (itemEffects.Count==0) return`） |
| 5 | DurabilitySystem maxDurability 检查 | ❌ | `ConsumeDurability:82` 仍只检查 `hasDurability`，**未加 maxDurability > 0 检查** |
| 6 | Inventory storageWidth/Height 兜底 | ⚠️ | EquipItem:233 有兜底（`>0 ? value : 1`）；**ApplyEquip:180 无兜底**（直接赋值，0 值会塌缩） |
| 7 | ItemGraphBuilder 引用完整性检验 | ✅ | ValidateReferences() 已实现（同系统接入规范 #1） |

### 审查时发现的遗漏系统（不在原清单，但已做）

| 系统 | 状态 | 证据 |
|------|:--:|------|
| AIBotUI 7处硬编码 | ✅ 已改 | `energyValue > 0` 替代"电池组/浓缩铀" / `repairValue > 0` 替代"高级零件/铁锭" / `fusionCoreSize` 替代"聚变核心(小/大)" |
| AIBot.cs 聚变核心 | ✅ 已改 | `coreSize == FusionCoreSize.Small \|\| itemName == "聚变核心(小)"`（字段优先 + 向后兼容） |
| ItemData 新字段 | ✅ 已加 | energyValue / repairValue / fusionCoreSize / ammoItemData（保留 ammoItemName 向后兼容） |

### 审查时发现的遗漏系统（仍未做）

| 系统 | 状态 | 证据 |
|------|:--:|------|
| AIBotInventory 按 itemName 查 | ❌ 未改 | `AIBotInventory.cs:132,144` 仍 `slot.itemData.itemName == itemName` |
| PlayerInteraction 遍历 placedItems | ❌ 未改 | `PlayerInteraction.cs:84-85` 仍直接遍历 containers/placedItems |
| WeaponShooting ammoItemData | ⚠️ 部分改 | `:184` 优先用 ammoItemData；`:277/287/298` 仍用 ammoItemName（未统一） |

**完成度: 3/7 已做 + 3/7 部分做 + 1/7 未做**

---

## 四、汇总

| 文档 | 完成度 | 关键遗留 |
|------|:--:|------|
| 系统接入规范 | **80%** | AIBotWeaponMapping.asset 未建（.cs 建了） |
| 武器与装备重设计划 | **70%** | 8 个装备 .asset 缺失（鞋子全缺）/ 3 个材料缺失 / 旧 RecipeData 未清理 |
| 物品系统关联升级 | **60%** | DurabilitySystem 未改 / 3 个遗漏系统未改 / 4 项只做了一半 |

### 🔴 必须处理的遗留问题（会导致运行错误）

1. **旧 RecipeData 未删** — 引用已删的旧物品 → resultItem 为 null → ItemGraphBuilder 引用完整性检验会报错，ItemGraph 重建会断裂
2. **ElectronicDevice.ReplaceBattery 逻辑错误** — `Flashlight && "电池组"` 永远不匹配 → 换电池功能失效
3. **AIBotWeaponMapping.asset 未建** — _weaponMapping 运行时为 null → AIBotCombat 实际走 fallback（硬编码仍在生效）
4. **DurabilitySystem 缺 maxDurability 检查** — hasDurability=true 但 maxDurability=0 的物品 → 耐久被扣但永不衰减

### 🟡 应补全的缺失内容

5. 8 个装备 .asset（鞋子 3 个 + 上衣 2 个 + 裤子/背心/腰带各 1 个）
6. 3 个材料 .asset（尼龙/凯夫拉/陶瓷板）
7. Inventory.ApplyEquip:180 加 storageWidth/Height 兜底
8. WeaponShooting 统一用 ammoItemData（:277/287/298 改用 :184 的 ammoName 变量）
9. AIBotInventory / PlayerInteraction 改用 Inventory API（消除 placedItems 直接访问）
10. SurvivalSystem 纯 foodType 触发识别（当前需 itemEffects 或 healAmount 才进入处理）
