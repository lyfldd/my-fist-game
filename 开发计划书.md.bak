# 🧟 僵尸生存游戏 — 四阶段开发计划书（3D 版）

> **团结引擎 + 3D 世界 + 45° 俯视摄像机 · 类《Project Zomboid》**
> 设计哲学：末日生存写实化，关键处深、繁复处简
> 开发节奏：阶段 2 搭骨架（代码/数据）→ 阶段 3 填血肉（内容）→ 阶段 4 上颜值（美术/UI）

---

## 🏗️ 四阶段总览

```
阶段 1      阶段 2             阶段 3            阶段 4
地基        骨架（代码+数据）   血肉（内容填充）   颜值（美术/UI）
──────────────────────────────────────────────────────────
移动 ✅     物品系统 ✅         物品数据扩展       3D 模型
摄像机 ✅   背包系统 ✅         僵尸类型            UI 面板
碰撞 ✅     装备系统 ✅         配方扩展            动画
EventBus ✅ 交互系统            地图填充            材质/特效
           时间系统             任务/事件            图标/贴图
           人物系统             Loot 分布            音乐/音效
           世界容器             故事线
           生存数值             世界生成
           战斗系统 ✅
           建造系统
           交通工具
           配方/制作
           电力/水源
           区域/建筑
```

---

## ✅ 阶段 1：地基（已完成）

| 步骤 | 内容 |
|------|------|
| 1.1 项目创建 | 团结引擎 3D URP |
| 1.2 目录结构 | Assets/_Game 分层 |
| 1.3 EventBus | 泛型事件系统 |
| 1.4 3D 地面 | Plane + Cube 建筑 |
| 1.5 角色移动 | Capsule + Rigidbody + XZ |
| 1.6 45° 摄像机 | LateUpdate 平滑跟随 |
| 1.7 碰撞阻挡 | 3D Collider |

---

## 🔄 阶段 2：世界骨架（代码 + 数据）

> 核心原则：**按依赖层级开发，不跳级**。
> L1 → L2 → L3 → L4，每一层为上一层提供材料。

### 层级总图

```
L1 地基 ─── 任何系统都依赖它
  ├─ ItemData (SO) ✅     物品是一切的基础
  ├─ EventBus ✅          系统间通信
  ├─ 移动/摄像机 ✅       玩家操作基础
  └─ 碰撞/物理 ✅         世界交互基础

L2 框架 ─── L3/L4 都要用它
  ├─ 背包系统 ✅          所有物品存放的地方
  ├─ 装备系统 ✅          Dictionary数据驱动，容器尺寸、属性加成
  ├─ 交互系统 ✅          按 E 与世界互动
  ├─ 时间系统 ✅          日夜循环、Tick 驱动
  ├─ 人物系统 ✅          性别/职业/属性/成长
  └─ 物品使用系统 ✅      ItemUsageSystem，解耦UI与系统层

L3 机制 ─── 具体的玩法系统（由 L2 驱动）
  ├─ 世界容器 ✅          柜子/冰箱/尸体（地表物品✅, Phase2完成）
  ├─ 生存数值 ✅          Phase1核心完成
  ├─ 战斗系统 ✅          Phase1~6全完成（武器数据+装备+模型+瞄准+射击+伤害）
  ├─ 建造系统 ✅          Phase1完成（Ghost预览+放置+拆除+BuildMenuUI+7默认资产）
  └─ 交通工具 ✅          Phase1完成（驾驶+上车/下车+摄像机跟随+油量+SHIFT加速+防侧翻）

L4 组装 ─── 需要多个 L3 配合
  ├─ ChunkManager ⬜     80m区块加载+容器刷新（L4最高优先级基础设施）
  ├─ ContainerRegistry ⬜ 懒加载+两态追踪+区块卸载时释放内存
  ├─ 僵尸 AI ⬜           状态机 + NavMesh（依赖 ChunkManager 激活/休眠）
  ├─ 配方/制作 ⬜          Recipe SO + 工作站
  ├─ 电力/水源 ⬜         发电机/水管/过滤
  └─ 区域/建筑 ⬜         分区/Loot/建筑模板（依赖 ChunkManager + ContainerRegistry）
```

> ✅ **2026-05-15 Phase 1 完成**：三层模块化架构 + WFC 约束求解 + 40m基础单元。
>
> | 层级 | 尺寸 | 内容 | 约束 | 弯曲感 | 状态 |
> |------|------|------|------|-------|------|
> | 一级(区块级) | 40~200m | 商业/住宅/工业/郊区/道路/水域 | 邻接表+风格排列+等级数量 | 矩形网格，确定功能区位置 | **✅ Phase 1** |
> | 二级(建筑级) | 40/56/72m | 90+建筑/地块模块 | 类型继承+道路接口+密度缩放 | **弯曲来源**（弯道/T字路/转角） | 未来 |
> | 三级(室内级) | 3m起 | 房间/家具 | 外墙开窗+楼梯贯穿+家具朝向 | — | 未来 |
>
> **网格架构**：40m基础单元 × 20×20网格 = 800m×800m 世界。模块占多格：道路1×1(40m)，商业/住宅3×3~4×4(120~160m)，工业/郊区5×5(200m)。
> WFC坍缩选 `ModuleCandidate(type+w+h)`，footprint占用检查 + perimeter约束传播 + 财富等级配额管理。
> 弯曲逻辑：一级只定矩形区块，二级模块（弯道路段/T字路口/转角建筑）注入弯曲感。
>
> 📌 完整设计：`程序化城市生成算法文档.md`
> Pipeline: Seed(10) → CityLayout(20) → ModuleGrid(30) → ModuleAssignment(35) → Mesh(80)

---

### L1：地基 ✅ 全部完成

### L2：框架 ✅ 全部完成

#### 2.1 物品系统 ✅ Phase 1
| 材料 | 说明 |
|------|------|
| ItemData SO | 物品数据结构 |
| ItemCategory 枚举 | 分类 |
| EquipSlot 枚举 | 装备位 |
| CreateDefaultItems.cs | 一键生成所有物品资产 |
| **优化：运行时物品实例** | 物品增加变质/耐久等运行时状态 → 阶段3 |
| **优化：更多属性字段** | 可扩展的行为配置 → 阶段3 |

#### 2.2 背包系统 ✅ Phase 1
| 材料 | 说明 |
|------|------|
| InventoryContainer | 单容器独立网格+物品 |
| Inventory | 多容器管理 + 总负重 |
| PlacedItem | 矩形寻位数据结构 |
| EquippedItems | 装备槽结构体 |
| OnValidate | 防误装校验 |
| 总面板 UI | Tab→标签栏+内容区（骨架） |
| 快捷面板 UI | V/4-8 快捷键（骨架） |
| 浮动提示 | Toast 超载/格满 |
| **优化：容器 UI 拖拽完善** | 跨容器拖拽物品、右键菜单 → 阶段3 |
| **优化：自动整理** | 按类型/重量排序、堆叠合并 → 阶段3 |
| **优化：堆叠拆分** | ALT+点击拆堆 → 阶段3 |

#### 2.3 装备系统 ✅ Phase 1
| 材料 | 说明 |
|------|------|
| equipSlot 校验 | OnValidate 自动清错装 |
| 容器扩容 | 穿装备→容器变大 |
| 脱下装备 | 清空容器+恢复默认尺寸 |
| **优化：装备模型显示** | 穿装备在角色模型上可见 → 阶段4 |
| **优化：更多装备槽** | 戒指/副手/挂件 → 阶段3 |

#### 2.4 交互系统 ✅ Phase 1
| 材料 | 说明 | 被谁依赖 |
|------|------|---------|
| IInteractable 接口 | 所有可交互物体实现此接口 | 世界容器/建造/车辆 |
| 交互检测 | 玩家面向+距离检测 | 同上 |
| 按 E 交互 | 统一交互键 | 同上 |
| 交互提示 | 靠近显示"按 E 打开" | 同上 |
| 搜索进度条 | 打开容器→转圈→显示内容 | 世界容器 |
| **优化：E 检视 / F 快速拾取** | 交互区分查看 vs 拾取 → 阶段3 与物品联动一起做 |
| **优化：可交互物体高亮** | 靠近时高亮/描边 → 阶段4 |
| **优化：交互范围可视化** | 调试用显示半径 → 可跳过 |

#### 2.5 时间系统 🔄（Phase 2 升级中）
| TimeManager | 游戏时间加速 + 天数追踪 | 所有 Tick + ChunkManager | ✅ Phase 1 |
| 日夜循环 | Directional Light 旋转 | 僵尸 AI/UI | ✅ Phase 1 |
| 天数追踪 | `currentDay` + `DayChanged` 事件 | ChunkManager 刷新冷却 | 🔄 Phase 2 开发中 |
| 时间事件 | 每天特定时间触发 | 任务 | 二期 |
| **优化：时间控制** | 暂停/倍速 (0/1/2/5) → 阶段3 | — | 二期 |
| **优化：日历系统** | 月份/季节/星期 → 阶段3 | — | 二期 |
| **优化：天气系统** | 晴天/雨天/雾天 → 阶段4 | — | 三期 |
| **优化：季节系统** | 温度影响 + 视觉变化 → 阶段4 | — | 三期 |
| **重构：UI 解耦** | 删除内嵌 OnGUI，迁至 TopLeftHUD → 阶段2 L4 | — | 🔄 开发中 |

> **📋 一期升级内容**: `currentDay` float + `DayChanged` 事件 + 天数UI。仅1文件改动+1事件结构体。

#### 2.6 人物系统 🔄 Phase 2 重构中

> **状态**: 旧版 Phase1 已完成（4属性+6技能）。Phase 2 全面重构为 5大类层级体系。详见下方完整计划。
> **日期**: 2026-05-20 定稿

##### 一、身体属性（4个，移除智力）

| 属性 | 简称 | 决定 |
|------|------|------|
| 力量 | STR | 近战伤害、负重上限 |
| 敏捷 | AGI | 移动速度、闪避率、潜行效果 |
| 体质 | CON | 生命值上限、自然恢复速度 |
| 耐力 | END | 体力上限、体力恢复速度 |

##### 二、战斗技能（3个）

| 技能 | 决定 | 联动 |
|------|------|------|
| 近战专精 | 近战伤害加成 + 近战闪避 | PlayerCombat 伤害公式 |
| 枪械专精 | 瞄准精度（散射锥收窄） | WeaponAiming.spread × (1-等级×0.06) |
| 防御专精 | 减伤率 | 联动装备护甲值 |

##### 三、生存技能（3个）

| 技能 | 决定 | 联动 |
|------|------|------|
| 资源采集 | 伐木/挖矿/搜刮速度 + 稀有物品爆率 | 容器 LootTable 加权 |
| 医疗生存 | 医疗品效果加成、debuff 处理、草药制作解锁 | SurvivalSystem |
| 野外求生 | 追踪/狩猎/陷阱制作、环境适应（抗寒/耐热/夜视） | 天气+温度系统（未来） |

##### 四、制造/工程技能（3个）

| 技能 | 决定 | 联动 |
|------|------|------|
| 工匠制作 | 工具/武器制作品质、改造配件 | RecipeData 品质产出 |
| 建造拆解 | 建造耗时缩短、材料消耗减少、拆解返还提高 | BuildModeController |
| 汽车改造 | 载具改装解锁（等级门槛） | VehicleController（未来） |

##### 五、心智（1个）

| 技能 | 决定 | 联动 |
|------|------|------|
| 智力 | 配方解锁门槛、高阶制作品质、NPC 说服（未来） | RecipeData.minIntelligence |

##### 六、技能配合规则

- 三项核心路线：**生存 + 建造 + 战斗**，心智做全局辅助
- 跨技能联动示例：自制土枪 = 工匠制作 Lv3 + 智力 Lv2；汽车改装 = 汽车改造 Lv2 + 智力 Lv3 + 建造拆解 Lv2
- 身体属性影响战斗：力量→近战伤害、敏捷→闪避、体质→血量

##### 七、生存经验系统（全局统一）

```
所有行为 → 生存经验（全局单一池）→ 100XP = 1技能点 → 自由兑换任意技能升级
```

**核心机制：**
- `SurvivalXPSystem` 单例管理全局XP池 + 可用技能点
- `AddXP(int amount)` — 所有系统统一调用
- `SpendPoint(SkillType)` / `SpendAttributePoint(AttributeType)` — 花点升级
- 同技能越高级越贵（见耗点表）

**升级耗点梯度：**

| 技能组 | Lv1→2 | Lv2→3 | Lv3→4 | Lv4→5 | ... | Lv9→10 |
|--------|--------|--------|--------|--------|-----|--------|
| 身体属性 | 5 | 5 | 6 | 6 | ... | 10 |
| 资源采集 | 10 | 10 | 11 | 12 | ... | 15 |
| 近战/枪械/防御 | 10 | 12 | 14 | 16 | ... | 25 |
| 野外求生 | 15 | 17 | 19 | 22 | ... | 30 |
| 医疗生存 | 25 | 27 | 30 | 33 | ... | 42 |
| 工匠制作 | 35 | 37 | 40 | 43 | ... | 52 |
| 建造拆解 | 35 | 37 | 40 | 43 | ... | 52 |
| 汽车改造 | 40 | 43 | 46 | 49 | ... | 57 |
| 智力 | 50 | 53 | 57 | 61 | ... | 80 |

公式：`cost = baseCost + currentLevel × incrementStep`

**经验获取行为清单（10大类）：**

| 类别 | 行为示例 | XP量（参考） |
|------|---------|:---------:|
| 时间自然 | 每游戏日 / 每小时挂机 / 夜间×2 | 200 / 5 / ×2 |
| 移动探索 | 徒步 / 奔跑 / 攀爬 / 新区域 / 搜索房间 | 2~30 |
| 身体劳作 | 搬运 / 负重行走 / 伐木 / 挖矿 / 采集 / 剥皮 | 3~12 |
| 战斗击杀 | 普通丧尸 / 精英丧尸 / 野兽 / 爆头 / 格挡 / 闪避 | 5~60 |
| 生活生存 | 生火 / 净水 / 烹饪 / 包扎 / 救助队友 | 5~15 |
| 建造制作 | 合成材料 / 制作武器 / 放置建筑 / 陷阱 / 拆解 | 10~25 |
| 机械载具 | 驾驶 / 越野 / 修理 / 启动发电机 | 3~20 |
| 心智知识 | 阅读书籍 / 学习蓝图 / 破译密码 | 30~50 |
| 交互社交 | 对话 / 委托 / 交易 / 组队（未来 NPC） | 待定 |
| 特殊事件 | 空投 / 随机事件 / 高危探索 / 成功逃生 | 待定 |

##### 八、体力系统（StaminaSystem）

```
最大体力 = 100 + 耐力 × 10
跑步消耗: 8/s
近战攻击: 15/次
伐木/挖矿: 10/次
负重行走（>50%负重）: 消耗×1.5

静止恢复: +15/s
走路恢复: +10/s
耐力加成: 恢复速度 += 耐力 × 2

体力=0 → 无法跑步、无法攻击、移速×0.6
```

##### 九、文件清单

**新建：**

| 文件 | 说明 |
|------|------|
| `Systems/Character/SurvivalXPSystem.cs` | 全局经验池单例：AddXP / SpendPoint / 自动换算技能点 |
| `Systems/Character/StaminaSystem.cs` | 体力组件：消耗/恢复/耐力联动/疲惫状态 |
| `Systems/Character/SkillCostTable.cs` | 静态升级耗点查询表 |
| `Editor/UpdateCharacterData.cs` | 一键更新旧 .asset 到新枚举结构 |
| `Editor/CreateDefaultProfessions.cs` | 重新生成3职业模板（新技能加成） |

**改造：**

| 文件 | 改动 |
|------|------|
| `Config/CharacterData.cs` | 枚举重写：AttributeType(4) + SkillType(10)；默认列表更新；移除智力 |
| `Config/CharacterTemplate.cs` | SkillBoost 下拉自动适配新枚举（无代码改动） |
| `Systems/Character/PlayerCharacter.cs` | 移除 Intelligence + AddSkillXp()；新增 Endurance；GetSkillLevel 读 SurvivalXPSystem |
| `Core/GameEvents.cs` | SkillXpGained → SurvivalXpGained |
| `Core/GameConstants.cs` | +40个XP常量 + 体力常量 + 公式常量 |
| `Systems/Combat/DamageableZombie.cs` | SkillXpGained.Publish → SurvivalXPSystem.AddXP |
| `Systems/Combat/PlayerCombat.cs` | 伤害公式：力量×0.05 + 近战专精×0.08 |
| `Systems/Weapon/WeaponAiming.cs` | 散射收窄：spread × (1 - 枪械专精×0.06) |
| `Systems/Weapon/WeaponShooting.cs` | 同上 |
| `Systems/Inventory/Inventory.cs` | 负重上限 += 力量 × 5 |
| `Systems/Building/BuildModeController.cs` | 修 TODO：技能检查生效 + 建造完成给经验 |
| `UI/DebugPanel.cs` | 新属性+技能+经验池显示 |

##### 十、构建顺序（11步）

```
1. 枚举层: CharacterData.cs 枚举重写
2. 配置层: SkillCostTable + GameConstants + GameEvents 改造
3. SO数据层: CharacterData/CharacterTemplate 默认值更新 + 编辑器工具
4. 运行时角色: PlayerCharacter 改造
5. 经验系统: SurvivalXPSystem 完整实现
6. 体力系统: StaminaSystem 完整实现
7. 系统接入: 战斗/武器/背包/建造 全项目改造
8. PlayerController 接入体力检查
9. TimeManager 接入时间经验
10. DebugPanel 更新显示
11. 计划书更新
```

---

#### 2.7 职业系统 ⬜ Phase 1 设计完成

> 状态: 设计定稿，待实现。依赖 2.6 人物系统重构（已完成）。
> 日期: 2026-05-20

**设计概述**: 职业是角色创建的入口选择，决定初始属性修正、技能加成、开局装备和随身物品。8个预设职业覆盖不同玩法路线 + 1个彩蛋角色（制作人）。

**执行顺序（关键）**: 必须先穿装备扩容容器，再放物品。
```
1. InitializeContainers()      → 容器默认尺寸(1x1~2x2)
2. ApplyProfession()           → 属性修正 + 技能加成
3. EquipStartingGear()         → 穿装备 → 容器扩容
4. AddStartingItems()          → 填物品 → 放入已扩容容器
```

**CharacterTemplate 新增字段**:
```csharp
public ItemData[] startingEquipment;    // 开局装备（EquipItem 自动扩容）
public ItemRequirement[] startingItems; // 开局随身物品（扩容后放入）
```

---

**8个预设职业**:

| # | 职业 | 属性修正 | 技能加成 | 装备 | 定位 |
|---|------|---------|---------|------|------|
| 1 | 消防员 | STR+2 CON+1 | 近战+2 防御+1 | 无 | 近战肉盾 |
| 2 | 警察 | AGI+1 | 枪械+3 防御+1 | 牛仔裤 | 枪械开局 |
| 3 | 医生 | — | 医疗+4 智力+2 | T恤 | 医疗专精 |
| 4 | 工程师 | — | 工匠+3 建造+3 智力+2 | T恤 | 制造建造 |
| 5 | 士兵 | STR+2 AGI+1 END+1 | 近战+2 枪械+2 防御+2 | 夹克+裤+腰封+头盔 | 战斗全能 |
| 6 | 伐木工 | STR+3 END+2 | 采集+3 近战+2 | 牛仔裤 | 采集战斗 |
| 7 | 流浪者 | END+3 CON+2 | 野外+4 近战+1 | 无 | 野外生存 |
| 8 | 技工 | — | 汽车+4 工匠+2 智力+1 | T恤+皮带 | 载具专精 |

---

**彩蛋角色: "制作人" (Creator)** — 开局即巅峰，用于测试/展示。

| 类别 | 内容 |
|------|------|
| 属性 | STR 9 / AGI 8 / CON 9 / END 9 |
| 技能 | 近战5 枪械5 防御3 采集3 医疗2 野外3 工匠5 建造5 汽车3 智力8 |
| 装备 | 战术夹克+战术裤+战术腰封+战术背心+军用背包+防弹头盔 (62护甲) |
| 武器 | AK47(主手) + 长刀(近战) |
| 物品 | 绷带x5 抗生素x3 夹板x3 止痛药x10 能量棒x8 罐头x10 饮用水x5 咖啡x6 钉子x30 木头x20 破布x10 |
| 特质 | "上帝之手" — 制作/建造速度 ×1.5 |

---

**文件清单**:

| 类型 | 文件 | 说明 |
|------|------|------|
| 新建 | `Systems/Character/ProfessionApplier.cs` | 职业应用组件：EquipStartingGear → AddStartingItems |
| 新建 | `Editor/CreateProfessionTemplates.cs` | 一键生成 9 个 CharacterTemplate .asset |
| 改造 | `Config/CharacterTemplate.cs` | 新增 startingEquipment / startingItems 字段 |
| 改造 | `Systems/Character/SurvivalXPSystem.cs` | ApplyProfession() 接入初始物品投放 |

**构建顺序**: 1.CharacterTemplate加字段 → 2.ProfessionApplier组件 → 3.SurvivalXPSystem接入 → 4.编辑器生成9个模板 → 5.手动创建CharacterData绑定职业测试

**Phase 2 展望**: 职业专属特质(被动技能) + 职业锁定配方 + 负面特质系统 + 职业选择UI(新游戏角色创建界面)

---

### L3：机制（全部待开发）

#### 3.1 世界容器 ✅ Phase 2 完成
| 材料 | 说明 | 状态 | 依赖 |
|------|------|:----:|------|
| 【Phase 1】地表物品 | 地面丢物品 WorldItem + Billboard + GroundItemInteract | ✅ | L2 背包 + 交互 |
| 【Phase 2】容器组件 | 柜子/冰箱/尸体 → 搜索进度条 → 打开容器格子 | ✅ | L2 背包 + 交互 |
| 容器 Loot | LootTable 权重随机 + ContainerLootProfile 配置 | ✅ | L1 物品 |
| 容器状态 | Unopened → Opened 两态（不再有Empty，永远可交互） | ✅ | - |
| ContainerRegistry | 懒加载：未搜过零开销，搜过后才建 InventoryContainer | L4 | L4 ChunkManager |
| 4 默认 Profile | FridgeProfile/CabinetProfile/CorpseProfile/CrateProfile | ✅ | Editor 一键创建 |
| 容器刷新 | 区块级统一刷新：Chunk重载→冷却到→该Chunk所有容器重置Unopened | L4 | L4 ChunkManager |
| **优化：同物品堆叠合并** | 地面上同种物品合并显示 | 阶段3 一起做 | - |
| **优化：对象池** | 大量掉落物复用对象 | 阶段3 内容填充时 | - |
| **优化：3D 模型代替贴图** | 地面物品用模型代替 Quad 贴图 | 阶段4 颜值 | - |
| **优化：腐烂消失** | 物品随时间腐烂 | L3 生存数值联动 | - |

#### 3.2 生存数值 ✅ Phase 1 完成
| 材料 | 说明 | 状态 | 依赖 |
|------|------|:----:|------|
| 【Phase 1】核心系统 | SurvivalSystem.cs — 4 属性 + Tick 衰减 + 危险伤害 + 状态系统 | ✅ | L2 时间 + 人物 |
| 【Phase 1】食物/水源 | ApplyItemEffect 接口（物品食用效果） | ✅ | L2 背包 |
| 【Phase 1】UI HUD | SurvivalHUD.cs — 4 属性条自动创建，屏幕左下角 | ✅ | 事件驱动更新 |
| 伤病系统 | 出血/感染/骨折 完整治疗流程 | ⬜ | L2 人物(CON) |
| 温度系统 | 环境+衣物+体温 | ⬜ | L2 人物 |
| 水源系统 | 口渴、打水、烧水、污染 | ⬜ | L2 交互 |
| 水源过滤 | 脏→净、耗时+滤芯 | ⬜ | L2 背包 |
| **优化：UI 显示** | 血条/饱食/口渴/体温 HUD → 阶段3 与总面板一起做 |
| 【Phase 1】UI HUD | SurvivalHUD.cs — 4 属性条自动创建，屏幕左下角 | ✅ | 事件驱动更新 |
| **优化：物品变质/腐败** | 食物随时间变质 → 阶段3 |
| **优化：烹饪/加热** | 生→熟、加热食物 → 阶段4 |

#### 3.3 战斗系统 ✅ Phase 1~6 全部完成
| 材料 | 说明 | 状态 | 依赖 |
|------|------|:----:|------|
| 【Phase 1】IDamageable 接口 | 统一伤害接口（玩家/僵尸/门/车窗） | ✅ | 无 |
| 【Phase 1】PlayerCombat | 左键近战：OverlapSphere 前方检测 + 冷却 | ✅ | L1 物理 |
| 【Phase 1】DamageablePlayer | 玩家受伤组件，转发伤害→SurvivalSystem | ✅ | L2 生存 |
| 【Phase 1】DamageableZombie | 僵尸受伤：闪红+死亡+经验事件+销毁 | ✅ | L2 人物 |
| 【Phase 2】武器数据 | isFirearm/weaponDamage/fireRate/spread... | ✅ | L1 物品 |
| 【Phase 2】EquipSlot 武器槽 | RightHand/LeftHand/KnifeBelt/SidearmBelt | ✅ | L2 装备 |
| 【Phase 3】WeaponHolder | 武器模型挂载：骨骼/Capsule模式+生命周期 | ✅ | L2 装备 + EventBus |
| 【Phase 4】WeaponAiming | 鼠标→3D射线+辅助瞄准2m+地面平面求交 | ✅ | L1 摄像机 |
| 【Phase 5】WeaponShooting | 锥形散射(Rodrigues)+射线命中+伤害+散射恢复 | ✅ | L1 物理 + WeaponAiming |
| 【Phase 6】武器配置 | 4把武器数据：木棍/长刀/沙鹰/AK47（.asset） | ✅ | L1 物品 |
| **优化：近战武器模型碰撞** | 刀剑用模型碰撞体代替 OverlapSphere → 阶段3 |
| **优化：弹道线/枪口火焰** | 射击视觉特效 → 阶段4 |
| **优化：击退/硬直** | 命中反馈+受击动画 → 阶段3 |
| **优化：耐久度** | 武器磨损→损坏 → 阶段3 |

#### 3.4 建造系统 ✅ Phase 1
| 材料 | 说明 | 依赖 |
|------|------|------|
| BuildableItem SO | 蓝图/材料/耗时/工具 | L1 物品 |
| 放置预览 | Ghost 跟随鼠标 | L1 物理 |
| 材料检查 | 背包材料是否足够 | L2 背包 |
| 消耗建造 | 扣材料→实例化 | L2 背包 + 交互 |
| 拆除系统 | 工具判定→返还材料 | L2 交互 |
| 建造 UI | BuildMenuUI + 7 种默认资产 | - |
| **优化：加固系统** | 门窗加木板 → 阶段3 |
| **优化：更多建筑类型** | 围墙/地板/家具 → 阶段3 |

#### 3.5 交通工具 ✅ Phase 1
| 材料 | 说明 | 依赖 |
|------|------|------|
| VehicleData SO | 速度/扭矩/油耗/后备箱/座位 | L1 物品 |
| VehicleController | Rigidbody + 4 WheelCollider 物理 | L1 物理 |
| VehicleInteraction | IInteractable（E键上车/下车） | L2 交互 |
| VehicleInputLock | 驾驶时禁用行走/武器/交互，WASD重映射 | L2 移动/武器/交互 |
| 摄像机切换 | CameraFollow.SetTarget 切换到车辆，距离+5m | L1 摄像机 |
| 油量系统 | 存油+耗油，SHIFT加速1.5倍油耗 | - |
| SHIFT加速 | 两档速度：普通maxSpeed / SHIFT=maxSpeed×2 | - |
| 防侧翻 | 反侧倾杆+重心下移+角速度阻尼，三层防护 | L1 物理 |
| 后备箱 | VehicleData 配置 trunkWidth×trunkHeight | L2 背包 |
| **重构：UI 解耦** | 删除内嵌 OnGUI，车速/油量迁至 TopLeftHUD → 阶段2 L4 | 🔄 开发中 |
| **优化：加油交互** | 油桶→车 → 阶段3 |
| **优化：耐久度/撞损** | 血量+碰撞伤害 → 阶段3 |
| **优化：更多车型** | 轿车/皮卡/面包车 参数差异 → 阶段3 |

---

### L4：组装

#### 4.0 ChunkManager + ContainerRegistry ✅（L4 最高优先级基础设施 · Phase1 已完成）

> **Git**: `ChunkManager(L4): Phase1 完成 — 三级区块+预热队列+RefreshHub+容器两态+性能分档+引导启动+鼠标朝向+角色模型目录`
> **日期**: 2026-05-19

---

## 📋 4.0 前置初始计划（动工前定稿）

> **模块**: L4 组装 · **步骤**: 第1步 · **名称**: ChunkManager + ContainerRegistry + RefreshHub — 三级区块管理与通用世界刷新
> **日期**: 2026-05-19

### 一、本步骤核心开发目标

将游戏中所有场景对象纳入 **80m 区块管理**，实现三级区块体系（已加载 / 预加载 / 未加载），玩家周围按需激活、异步预热、远距离休眠。同时改造 WorldContainer 为两态懒加载，并建立可扩展的 RefreshHub 刷新中

---

### 二、关联联动模块

| 关联度 | 系统 | 数据互通方式 |
|--------|------|-------------|
| **强** | WorldContainer | 改造 Awake→懒加载，状态机 3态→2态，通过 ContainerRegistry 获取容器 |
| **强** | WorldGen/WorldData | 复用 40m 基础单元网格，Chunk 坐标 = WorldData 网格坐标 / 2 |
| **中** | 僵尸 AI | 僵尸按坐标分配到 Chunk，三级体系管理（未来） |
| **中** | WorldItem (地面物品) | 地面物品分配到 Chunk，远距离 SetActive(false) |
| **弱** | 建造系统 | 建筑分配到 Chunk，GameObject 持久化，不额外序列化 |
| **弱** | 交通工具 | 车辆不随 Chunk 休眠（数量少，全场景常驻 Active） |

**数据流向**:
```
ChunkManager → RefreshHub.OnChunkLoad(chunkId, day)
                 → ContainerRefreshHandler  → 容器刷新+重建
                 → TreeRefreshHandler       → 树木再生（未来）
                 → BerryRefreshHandler      → 浆果重生长（未来）
ChunkManager → RefreshHub.OnChunkUnload(chunkId)
                 → 倒序调用各 Handler 清理
ChunkManager → 僵尸/建筑: SetActive 控制
```

**EventBus 事件**:
```
ChunkEnterLoadEvent   { int chunkId, Vector2 center }   // 进入已加载层
ChunkExitLoadEvent    { int chunkId }                     // 退出已加载层
ChunkEnterPreloadEvent { int chunkId }                    // 进入预加载层
ChunkUnloadedEvent    { int chunkId }                     // 完全卸载
ContainerRefreshedEvent { int containerId }               // 容器批量刷新完成
```

---

### 三、整体实现流程拆解

#### 3.1 初始化流程

```
场景加载 / WorldGen 完成后:
  ChunkManager.Initialize()
    → 100 个 Chunk 父节点 (10×10 网格): 命名 Chunk_{x}_{z}
    → 遍历场景中所有标记了 ChunkManaged 的对象:
        WorldContainer / WorldItem / 僵尸 / 建筑 / 地面物品
    → 按坐标计算 chunkId = ((int)(x/80), (int)(z/80))
    → 将对象 SetParent 到对应 Chunk
    → 各系统建立索引:
        ContainerRegistry.Register(containerId, chunkId, profile)
        ZombieSystem 预留 Register(zombieId, chunkId)  // 未来
        WorldItemSystem 标记 isPlayerDropped 字段

  RefreshHub.Initialize()
    → Register(new ContainerRefreshHandler())
    → // 未来: Register(new TreeRefreshHandler())
```

#### 3.2 运行时三级切换

```
每帧或每2秒（玩家移动 ≥ 20m 或 2秒间隔）:

  计算玩家所在 Chunk
  计算动态预热带宽度 = BasePreloadChunks + GetDynamicExtra(speed)

  遍历 100 个 Chunk:

    距离 ≤ 加载半径:
      if 状态 != Loaded:
        → 状态切为 Loaded
        → SetActive(true)
        → RefreshHub.OnChunkLoad(chunkId, currentDay)
        → EventBus.Publish(ChunkEnterLoadEvent)
        → 如果预热未完成，强制完成剩余步骤（同步）

    加载半径 < 距离 ≤ 加载半径 + 预热带宽:
      if 状态 == Unloaded:
        → 状态切为 Preloaded
        → 加入预热队列（分帧处理）
        → EventBus.Publish(ChunkEnterPreloadEvent)
      if 状态 == Loaded:
        → 状态切为 Preloaded
        → 僵尸 SetActive(false) + 状态重置（保留死亡掉落容器 Record）
        → 容器: 释放 InventoryContainer，保留 ContainerRecord
        → 建筑: SetActive(false)

    距离 > 加载半径 + 预热带宽:
      if 状态 != Unloaded:
        → 状态切为 Unloaded
        → RefreshHub.OnChunkUnload(chunkId)
            ContainerRefreshHandler: 释放 InventoryContainer，保留 Record
            僵尸: 丢弃全部 Record（死亡掉落容器除外，转为独立 Record）
        → SetActive(false) 整个 Chunk
        → EventBus.Publish(ChunkUnloadedEvent)
```

#### 3.3 异步预热队列

```
预热入口: Chunk 从 Unloaded 进入 Preloaded 层

预热队列:
  Queue<int> preloadQueue
  每帧处理 2 个预热步骤（preloadsPerFrame = 2）

状态机（每个 Chunk 内部）:
  Stage 0: SetActive(true) 建筑 + 容器 GameObject
  Stage 1: ContainerRefreshHandler 重建已搜过容器的 InventoryContainer
  Stage 2: 僵尸系统重生僵尸（未来）+ 掉落容器重建
  Stage 3: Done → 标记 ready，出队

  预热完成 → Chunk 在预加载层等待 → 玩家接近 → 已加载层瞬间完整

高速减载逻辑:
  玩家速度 > 20 m/s → 预热时跳过 Stage 2（僵尸+地面物品）
  玩家速度 > 40 m/s → 预热仅 Stage 0+1（建筑+容器）
  玩家急刹车（speed < 5 且上帧 > 20）→ 已加载层内补激活跳过的内容
```

#### 3.4 WorldContainer 交互流程（改造后）

```
WorldContainer.OnInteract(interactor):
  container = ContainerRegistry.GetOrCreate(containerId)

  if containerRecord.isOpened:
    → 直接打开容器窗口（瞬间，无读条）
  else:
    → 搜索读条协程
    → 进度条走完
    → 生成掉落（LootTable 随机）
    → ContainerRegistry.MarkOpened(containerId, currentDay)
    → 打开容器窗口

ContainerWindow关闭:
  → 不改变状态（不再有 Empty）
  → 空容器 = Opened + placedItems.Count == 0
  → 永远可交互
```

#### 3.5 容器刷新流程

```
RefreshHub.OnChunkLoad(chunkId, currentDay):
  ContainerRefreshHandler.OnChunkLoad(chunkId, day):
    遍历 ContainerRegistry 中该 Chunk 的所有 ContainerRecord:
      if isOpened && (currentDay - openedGameDay > profile.refreshCooldownDays)
      && profile.refreshEnabled:
        → 清空 InventoryContainer（重随机掉落）
        → 重置为 Unopened
        → 释放 InventoryContainer 引用
      elif isOpened:
        → 重建 InventoryContainer（保留玩家上次留下的物品）
      // else: Unopened → 什么都不做（保持懒加载）

规则:
  - 容器内所有物品（含玩家放入的）→ 全部刷新清空
  - 玩家自建存储容器 → 不刷新（未来处理，通过 refreshEnabled=false）
  - 尸体 → refreshEnabled=false，搜过永不重置
```

---

### 四、核心代码 / 架构设计思路

#### 4.1 新增文件

| 文件 | 说明 |
|------|------|
| `Systems/WorldGen/ChunkManager.cs` | MonoBehaviour 单例，每帧/定时检查玩家位置，驱动三级切换 + 预热队列 |
| `Systems/WorldGen/RuntimeChunk.cs` | `struct RuntimeChunk { int chunkId; Vector2Int gridPos; ChunkState state; PreloadStage preloadStage; List<int> containerIds; List<int> zombieIds; List<int> buildingIds; }` |
| `Systems/WorldGen/ChunkState.cs` | `enum ChunkState { Unloaded, Preloaded, Loaded }` |
| `Systems/WorldGen/ChunkQuality.cs` | `enum ChunkQuality { Low, Medium, High }` + 静态方法 `GetLoadRadius/GetPreloadRadius/GetUnloadRadius` |
| `Systems/WorldGen/RefreshHub.cs` | 刷新调度中心：`List<IRefreshHandler>` + `OnChunkLoad/OnChunkUnload` |
| `Systems/WorldGen/IRefreshHandler.cs` | 接口：`string handlerName` + `OnChunkLoad(chunkId, day)` + `OnChunkUnload(chunkId)` |
| `Systems/WorldContainer/ContainerRefreshHandler.cs` | 实现 IRefreshHandler，打理容器刷新+重建逻辑 |
| `Systems/WorldContainer/ContainerRegistry.cs` | MonoBehaviour 单例，`Dictionary<int, ContainerRecord>`，懒加载核心 |
| `Systems/WorldContainer/ContainerRecord.cs` | `class ContainerRecord { int containerId; bool isOpened; float openedGameDay; int chunkId; ContainerLootProfile profile; InventoryContainer container; }` |

#### 4.2 改造文件

| 文件 | 改造内容 |
|------|---------|
| `WorldContainer.cs` | 删除 Awake 中 `_container` 创建；删除 `ContainerState.Empty`；三态改两态（Unopened/Opened）；新增 `int containerId`；OnInteract 改为通过 `ContainerRegistry.GetOrCreate(containerId)` 获取容器；移除 `OnContainerWindowClosed` 中的 Empty 转换；移除 ContainerState 枚举（迁至 ContainerRecord） |
| `ContainerLootProfile.cs` | +`float refreshCooldownDays = 7` + `bool refreshEnabled = true` |
| `GameConstants.cs` | +`RUNTIME_CHUNK_SIZE = 80f` + `CHUNK_GRID_SIZE = 10` + `CHUNK_CHECK_DISTANCE = 20f` + `CHUNK_CHECK_INTERVAL = 2f` + `PRELOAD_STEPS_PER_FRAME = 2` + `PLAYER_DROP_PER_CHUNK_CAP = 400` + `SPEED_THRESHOLD_NORMAL = 10f` + `SPEED_THRESHOLD_FAST = 20f` + `SPEED_THRESHOLD_EXTREME = 40f` |

> **命名冲突处理**: GameConstants 已有 `CHUNK_SIZE = 32f`（旧 Mesh 用），新常量用 `RUNTIME_CHUNK_SIZE = 80f` 区分。旧常量保持不变。

#### 4.3 关键接口

```csharp
// ── 刷新处理器接口 ──
public interface IRefreshHandler {
    string handlerName { get; }
    void OnChunkLoad(int chunkId, float currentGameDay);
    void OnChunkUnload(int chunkId);
}

// ── 容器注册中心 ──
class ContainerRegistry : MonoBehaviour {
    Dictionary<int, ContainerRecord> _records; // 全量记录（初始化时创建）

    InventoryContainer GetOrCreate(int containerId);  // 懒加载
    void MarkOpened(int containerId, float gameDay);
    List<ContainerRecord> GetChunkRecords(int chunkId);
    bool IsOpened(int containerId);
}

// ── 刷新调度中心 ──
class RefreshHub : MonoBehaviour {
    List<IRefreshHandler> _handlers;

    public void Register(IRefreshHandler handler);
    public void OnChunkLoad(int chunkId, float currentDay);
    public void OnChunkUnload(int chunkId);
}

// ── 区块管理器 ──
class ChunkManager : MonoBehaviour {
    RuntimeChunk[] _chunks;          // 100 个
    Queue<int> _preloadQueue;
    ChunkQuality _quality;
    int _currentPlayerChunk;

    void CheckChunks();
    void LoadChunk(int chunkId);
    void PreloadChunk(int chunkId);  // 加入预热队列
    void UnloadChunk(int chunkId);
    bool ProcessOnePreloadStep(int chunkId);
    int GetDynamicPreloadExtra(float playerSpeed);

    // 静态辅助
    int GetChunkId(Vector3 worldPos);
    Vector2 GetChunkCenter(int chunkId);
}
```

---

### 五、解耦与可扩展预留设计

#### 5.1 分层架构

```
管理层: ChunkManager       — 坐标映射 + 激活控制 + 预热队列
调度层: RefreshHub         — 刷新 Handler 注册 + 遍历调度
数据层: ContainerRegistry  — 容器记录 + 懒加载 + 生命周期
        ZombieSystem       — 僵尸三级数据管理（未来）
        WorldItemSystem    — 地面物品持久化上限（未来）
业务层: WorldContainer     — 交互逻辑 + 搜索读条
        IRefreshHandler    — 各种刷新实现
```

#### 5.2 扩展预留

| 预留点 | 方式 | 用途 |
|--------|------|------|
| `IRefreshHandler` 接口 | 新增类型 = 实现接口 + RefreshHub.Register | 树木/浆果/野外物资/矿物等 |
| `RuntimeChunk.zombieIds` | `List<int>` | 僵尸系统管理自己的 Record |
| `RuntimeChunk.buildingIds` | `List<int>` | 建造系统管理，预留 |
| `RuntimeChunk.worldItemIds` | `List<int>` | 地表物品系统管理，预留 |
| `ContainerRecord.customData` | `Dictionary<string, object>` | 后续扩展字段，不影响现有序列化 |
| `ChunkQuality` 枚举 | 新增档位 = 加枚举值 + 对应半径 | 未来可加 Ultra 档 |
| `RefreshHub 独立于 ChunkManager` | Handler 不依赖 ChunkManager 实现细节 | 未来可单独测试刷新逻辑 |
| 新增刷新类型流程 | 3 步：Profile SO → Handler 实现 → Register | 不改 ChunkManager/ContainerRegistry |

---

### 六、玩家产出世界物品的持久化方案

#### 6.1 各类对象处理策略

| 对象类型 | 持久化方式 | 原因 |
|----------|-----------|------|
| **建造物 (PlacedStructure)** | GameObject 持久化，SetActive(false) 不销毁 | 本身有组件+Collider，Transform 保留 |
| **玩家丢弃物品 (WorldItem)** | GameObject 持久化 + 单 Chunk 400 上限 | 正常游玩 ~50 件，500 件才 ~1MB |
| **容器内物品** | 区块刷新时全部清空重随机 | 玩家自建存储容器除外（future） |
| **车辆** | 不随 Chunk 休眠，全场景常驻 | 数量少（5-10 辆），无性能影响 |

#### 6.2 地面物品上限兜底

```
单 Chunk 内 WorldItem 上限: 400 件
超出处理: FIFO，最旧的玩家丢弃物品 Destroy
天然生成的不计入上限（WorldGen 固定数量）
isPlayerDropped=true 的物品参与计数，天然生成的跳过
```

---

### 七、性能分级体系

#### 7.1 档位定义

```csharp
enum ChunkQuality { Low, Medium, High }

// 加载半径（Chunk 数）
Low    → 1 chunk  (80m)   // ~9 个活跃 Chunk
Medium → 2 chunks (160m)  // ~25 个活跃 Chunk ← 默认
High   → 3 chunks (240m)  // ~49 个活跃 Chunk

// 预热带基准 = 加载半径 + 1
// 卸载半径 = 预热带基准 + 1
```

| 档位 | 加载 | 预热带基准 | 卸载 |
|------|:---:|:--------:|:---:|
| 低 | 80m (1) | 160m (2) | 240m (3) |
| 中 | 160m (2) | 240m (3) | 320m (4) |
| 高 | 240m (3) | 320m (4) | 400m (5) |

#### 7.2 动态预热带（根据速度扩展）

```csharp
int GetDynamicPreloadExtra(float speed) {
    if (speed < 10f)  return 0;  // 步行
    if (speed < 20f)  return 1;  // 普通车
    if (speed < 40f)  return 2;  // 高速车
    return 3;                     // 极速
}
```

| 场景 | 预热带 | 卸载 |
|------|:-----:|:---:|
| 低配步行 | 160m | 240m |
| 中配步行 | 240m | 320m |
| 中配普通车 | 320m | 400m |
| 中配极速车 | 400m | 480m |
| 高配极速车 | 480m | 560m |

#### 7.3 高速减载策略

| 速度 | 预热内容 | 原因 |
|------|---------|------|
| 步行 (≤10 m/s) | 全量：建筑+容器+僵尸+地面物品 | 时间充裕 |
| 普通车 (≤20 m/s) | 全量 | 预热带够宽 |
| 高速车 (≤40 m/s) | 精简：建筑+容器，跳过僵尸+地面物品 | 飙车不停车打怪 |
| 极速 (>40 m/s) | 最简：仅建筑+容器 | 预热带窄，优先保建筑可见 |

> 高速减载内容在玩家减速时补激活：`speed < 5 && prevSpeed > 20` → 已加载层内补全跳过内容。

---

### 八、预期功能边界

**做什么**:
- ChunkManager 三级区块体系，100 Chunk 覆盖 800m×800m
- 异步预热队列，分帧激活，不卡主线程
- ContainerRegistry 懒加载：未搜过 = 零 InventoryContainer 内存
- 区块重载时按冷却天数批量刷新容器
- WorldContainer 两态：Unopened / Opened，永可交互
- RefreshHub 可扩展刷新接口
- 性能三档 + 动态预热带 + 高速减载

**不做什么**:
- 不做异步/协程加载（一期同步 SetActive + 预热队列已满足）
- 不做 LOD/渲染剔除（阶段4）
- 不做对象池（阶段3）
- 不做存档序列化（仅内存，存档延后）
- 不处理僵尸 AI 的 Chunk 归属（仅预留 zombieIds 列表）
- 不做地面物品刷新（仅保留一次，不重新生成）
- 不做车辆 Chunk 管理（全场景常驻）

---

### 九、调试与测试验收标准

| 验收项 | 标准 |
|--------|------|
| 玩家移动 | 移动到新 Chunk → 新区块内对象可见，离开 → 超出卸载半径后隐藏 |
| 三级切换 | Hierarchy 中可见 Chunk 父节点 on/off 跟随玩家位置 |
| 容器搜索 | 第一次打开 → 读条 → 生成掉落；第二次打开 → 瞬间显示 |
| 容器刷新 | 搜过容器 → 手动设 openedGameDay 为 7 天前 → Chunk 卸载再加载 → 容器回到 Unopened |
| 容器存物 | Opened 容器内放物品 → 卸载 → 未过冷却时加载 → 物品还在 |
| 容器全刷新 | 上述条件下过冷却 → 玩家物品也被清空，全家随机重来 |
| 内存 | 200 个容器场景：活跃 Chunk 内 ≤ 30 个有 InventoryContainer，其他未搜过零开销 |
| 预热不卡顿 | Hierarchy Profiler 确认 Awake/OnEnable 分散在多帧，单帧 ≤ 2 个预热步骤 |
| 高速减载 | 60 m/s 移动时主线程不卡，减速后僵尸/地面物品补激活 |
| 倒退兼容 | 旧 WorldContainer 场景对象改造后仍正常交互 |
| 性能分档 | 切换 Low/Med/High → 加载半径变化 → Hierarchy 中活跃 Chunk 数量对应变化 |
| 动态预热带 | 上车后预热带自动扩展 → 下车后恢复基准 |

---

### 十、新增刷新类型的未来流程

以后加"浆果刷新"只需 3 步，不动底层：

```
1. 创建 BerryProfile SO（含 refreshCooldownDays 等）
2. 创建 BerryRefreshHandler : IRefreshHandler
3. RefreshHub.Instance.Register(new BerryRefreshHandler())
→ 完成
```

---

### 十一、文件清单（更新后）

| 文件 | 状态 | 说明 |
|------|:----:|------|
| `Systems/WorldGen/ChunkManager.cs` | 新建 | 区块加载/卸载、坐标→Chunk映射、玩家位置追踪、预热队列 |
| `Systems/WorldGen/RuntimeChunk.cs` | 新建 | Chunk 数据结构（容器/僵尸/建筑 ID 列表） |
| `Systems/WorldGen/ChunkState.cs` | 新建 | `enum ChunkState { Unloaded, Preloaded, Loaded }` |
| `Systems/WorldGen/ChunkQuality.cs` | 新建 | `enum ChunkQuality` + 半径计算静态方法 |
| `Systems/WorldGen/RefreshHub.cs` | 新建 | 刷新调度中心：Handler 注册 + OnChunkLoad/Unload 遍历 |
| `Systems/WorldGen/IRefreshHandler.cs` | 新建 | 刷新处理器接口 |
| `Systems/WorldContainer/ContainerRefreshHandler.cs` | 新建 | 实现 IRefreshHandler，打理容器刷新+重建逻辑 |
| `Systems/WorldContainer/ContainerRegistry.cs` | 新建 | 容器注册中心、懒加载、GetOrCreate |
| `Systems/WorldContainer/ContainerRecord.cs` | 新建 | 容器记录（纯数据 class） |
| `Systems/WorldContainer/WorldContainer.cs` | 改造 | 三态→两态、Awake 移除容器创建、懒加载调用 Registry |
| `Config/ContainerLootProfile.cs` | 改造 | +`refreshCooldownDays`、+`refreshEnabled` |
| `Core/GameConstants.cs` | 改造 | +9 常量（RUNTIME_CHUNK_SIZE / CHUNK_GRID_SIZE / ...） |

> **Git 提交**: `ChunkManager(L4): Phase1 完成 — 三级区块+预热队列+RefreshHub+容器两态+性能分档+引导启动+鼠标朝向+角色模型目录`

---

### 4.0 后置定稿总结（开发完成后回顾）

#### 计划 vs 实际

| 对比维度 | 前置计划 | 实际完成 | 差异 |
|----------|---------|---------|:--:|
| ChunkManager | 三级区块 + 预热队列 + 性能分档 | 完全按计划实现 | ✅ |
| ContainerRegistry | 懒加载 + GetOrCreate + 两态追踪 | 完全按计划实现 | ✅ |
| RefreshHub | Handler 注册 + OnChunkLoad/Unload | 完全按计划实现 | ✅ |
| ContainerRefreshHandler | 容器冷却刷新 + 重建 | 完全按计划实现 | ✅ |
| WorldContainer 改造 | 三态→两态 | 完全按计划实现 | ✅ |
| 鼠标朝向 | 未在 4.0 初始计划中 | 额外完成：MouseGroundProjector + PlayerController 平滑旋转 | ➕ |
| UI 解耦 | 未在 4.0 初始计划中 | 额外完成：TopLeftHUD 上下文切换 | ➕ |
| 角色模型目录 | 未在 4.0 初始计划中 | 额外完成：Characters 目录结构 | ➕ |
| 僵尸 AI 集成 | 仅预留 zombieIds | RuntimeChunk + zombieInstances 列表，实际联动已在 4.1 完成 | ➕ |
| 高速减载 | 3 档速度阈值 | 按计划实现 | ✅ |
| 引导启动 | AutoRegisterContainers | 按计划实现 | ✅ |

#### 新建文件（实际）

| 文件 | 说明 |
|------|------|
| `Systems/WorldGen/ChunkManager.cs` | 三级区块管理器（单例，Awake 初始化保证在 Start 前就绪） |
| `Systems/WorldGen/ChunkState.cs` | `enum ChunkState { Unloaded, Preloaded, Loaded }` |
| `Systems/WorldGen/ChunkQuality.cs` | `enum ChunkQuality` + `ChunkQualityConfig` 半径计算 |
| `Systems/WorldGen/RuntimeChunk.cs` | Chunk 数据结构 + `PreloadStage` 枚举 + `zombieInstances` 列表 |
| `Systems/WorldGen/IRefreshHandler.cs` | 刷新处理器接口 |
| `Systems/WorldGen/RefreshHub.cs` | 刷新调度中心（单例） |
| `Systems/WorldContainer/ContainerRegistry.cs` | 容器注册中心（单例，懒加载 GetOrCreate） |
| `Systems/WorldContainer/ContainerRecord.cs` | 容器记录纯数据类 |
| `Systems/WorldContainer/ContainerRefreshHandler.cs` | 容器刷新处理器（实现 IRefreshHandler） |
| `Systems/PlayerInput/MouseGroundProjector.cs` | 鼠标→地面投影共享组件 |

#### 改造文件（实际）

| 文件 | 改造内容 |
|------|---------|
| `GameConstants.cs` | +9 Chunk常量 (RUNTIME_CHUNK_SIZE=80, CHUNK_GRID_SIZE=10, 预热/速度阈值) |
| `ContainerLootProfile.cs` | +refreshCooldownDays, +refreshEnabled |
| `WorldContainer.cs` | 三态→两态, Awake移除容器创建, 通过 ContainerRegistry 懒加载 |
| `PlayerController.cs` | +鼠标朝向旋转 |
| `WeaponAiming.cs` / `GhostPreview.cs` | 改用 MouseGroundProjector |

#### 经验教训

- **Awake vs Start 顺序问题**：ChunkManager.Initialize() 最初在 Start()，导致其他系统的 Start() 可能在 _chunks 就绪前调用 RegisterZombie。移到 Awake() 解决了所有依赖此数组的系统初始化顺序。
- **预留接口设计有效**：`IRefreshHandler` + `RefreshHub.Register` 模式让容器刷新零耦合接入，未来新刷新类型只需实现接口即可。

---

#### 4.1 僵尸 AI ✅（Phase1 已完成）

---

## 4.1 前置初始计划（Phase1 已完成）

> **模块**: L4 组装 · **步骤**: 第2步 · **名称**: 僵尸 AI — 有限状态机 + NavMesh + 集中式玩家感知
> **日期**: 2026-05-20

### 一、总体架构

```
┌─────────────────────────────────────────────────┐
│              ZombieAwarenessSystem               │
│           (单例，集中式玩家检测)                    │
│  每 0.2s 遍历活跃僵尸列表 → 距离判定 → 通知状态机    │
└────────────────────┬────────────────────────────┘
                     │ OnPlayerDetected / OnPlayerLost
     ┌───────────────┼───────────────┐
     ▼               ▼               ▼
  Zombie A       Zombie B       Zombie C
  (Chase)        (Wander)       (Idle)
     │               │               │
     └───────────────┴───────────────┘
                     │
              ZombieStateMachine (每只僵尸一个)
                     │
     ┌───────┬───────┼───────┬───────┐
     ▼       ▼       ▼       ▼       ▼
   Idle   Wander  Chase  Attack  Dead
  (5个全局单例，只存逻辑，零数据)
```

**三层分离设计**：

| 层 | 数量 | 职责 |
|----|------|------|
| 状态类 (ZombieState) | 5 个全局单例 | 只存逻辑，零数据。`Enter/Update/Exit` 的数据全从 ctx 读写 |
| 状态机 (ZombieStateMachine) | 每只僵尸 1 个 | 只存数据（timer / wanderTarget / playerDetected 等），零逻辑 |
| 感知系统 (AwarenessSystem) | 全局 1 个 | 集中距离检测，通知状态机，不分散到各僵尸 Update |

### 二、Chunk 绑定机制

```
僵尸出生 ──► 分配 chunkId ──► 加入 RuntimeChunk.zombieIds
                │
                ├─ Chunk 进入 Loaded ──► 状态机激活，Update 跑起来
                ├─ Chunk 退到 Preloaded ──► 状态冻结，NavMeshAgent 禁用
                └─ Chunk 退到 Unloaded ──► 完全不思考，SetActive(false)
```

任何时候 **只有 Loaded 层级的僵尸在跑 Update**。Preloaded/Unloaded 的僵尸零 CPU 消耗。

### 三、开发分两步

#### 第一步：核心骨架（今天做）

##### 3.1 `ZombieState` — 抽象基类

```csharp
public abstract class ZombieState
{
    public abstract void Enter(ZombieStateMachine ctx);
    public abstract void Update(ZombieStateMachine ctx);
    public abstract void Exit(ZombieStateMachine ctx);
}
```

##### 3.2 `ZombieStateMachine` — 状态机本体

挂僵尸 GameObject 上，职责：
- 持有 `NavMeshAgent`、`DamageableZombie`、当前状态引用
- `TransitionTo(ZombieState newState)` — Exit 旧状态 → Enter 新状态
- `Update()` 转发到当前状态
- 暴露 `Transform PlayerTarget`（由 AwarenessSystem 写入）
- 暴露 `bool PlayerDetected`（由 AwarenessSystem 设置）
- Chase 状态下每 0.5s 更新一次 `NavMeshAgent.destination`

**状态机上的数据字段**（状态类本身不存数据）：

```csharp
public class ZombieStateMachine : MonoBehaviour
{
    public ZombieState currentState;

    // 感知（由 AwarenessSystem 写入）
    public Transform playerTarget;
    public bool playerDetected;

    // Idle 用
    public float idleTimer;
    public float idleDuration;

    // Wander 用
    public Vector3 wanderTarget;

    // Attack 用
    public float attackTimer;

    // 组件引用
    public NavMeshAgent agent;
    public DamageableZombie damageable;
}
```

##### 3.3 五个状态实现

| 状态 | Enter | Update | Exit | 转换条件 |
|------|-------|--------|------|---------|
| **Idle** | `ctx.idleDuration = Random(1,4)`，站立 | 计时到 → Wander；PlayerDetected → Chase | — | timer 到 → Wander；发现玩家 → Chase |
| **Wander** | 以当前位置 5~10m 半径 NavMesh 随机采样，半速走过去 | 到达 → Idle；PlayerDetected → Chase | — | 到达 → Idle；发现玩家 → Chase |
| **Chase** | 全速，设 destination 为玩家位置 | 每 0.5s 更新 destination；距离 ≤ 1.5m → Attack；距离 > 30m → Wander | — | 贴脸 → Attack；丢失 → Wander |
| **Attack** | 停移动，重置计时器 | 每 1.5s 对玩家造成 10 伤害；面朝玩家；距离 > 1.5m → Chase；HP ≤ 0 → Dead | — | 脱离范围 → Chase；死亡 → Dead |
| **Dead** | 禁用 NavMeshAgent + 碰撞 | 等 Destroy（DamageableZombie 已有 1s 延迟） | — | 无 |

每个状态使用静态单例模式：

```csharp
public class ZombieState_Idle : ZombieState
{
    public static readonly ZombieState_Idle Instance = new();

    public override void Enter(ZombieStateMachine ctx)
    {
        ctx.idleTimer = 0f;
        ctx.idleDuration = Random.Range(1f, 4f);
    }

    public override void Update(ZombieStateMachine ctx)
    {
        ctx.idleTimer += Time.deltaTime;
        if (ctx.idleTimer >= ctx.idleDuration)
            ctx.TransitionTo(ZombieState_Wander.Instance);
        if (ctx.PlayerDetected)
            ctx.TransitionTo(ZombieState_Chase.Instance);
    }

    public override void Exit(ZombieStateMachine ctx) { }
}
```

##### 3.4 `ZombieAwarenessSystem` — 集中式玩家检测

方案 B。单例 MonoBehaviour，职责：
- `List<ZombieStateMachine> _activeZombies` — 僵尸 Awake 时注册，OnDestroy 时注销
- 每 0.2s 遍历一次 `_activeZombies`，算每个僵尸与玩家距离
- 距离 ≤ 10m 且之前未 detected → `stateMachine.OnPlayerDetected(playerTransform)`
- 距离 > 10m 且之前 detected → `stateMachine.OnPlayerLost()`
- 玩家 Transform 在 Start 时缓存一次（`GameObject.FindWithTag("Player")`）

```
每 0.2s 遍历:

  Zombie A (距离 8m)  → OnPlayerDetected()  // 首次发现
  Zombie B (距离 50m) → 无变化
  Zombie C (距离 3m)  → 已 detected，持续追击
  Zombie D (距离 12m) → OnPlayerLost()      // 刚超出范围
```

##### 3.5 改造 `ZombieController`

- 删除旧的 `Update()` 里直接移动逻辑（`transform.position +=`）
- 变成薄壳组件：挂 `NavMeshAgent`、`ZombieStateMachine`、`DamageableZombie`
- 初始化时创建状态机并注册到 AwarenessSystem

##### 3.6 改造 `DamageableZombie`

- `Die()` 方法中通知状态机进入 Dead 状态

##### 3.7 接入 ChunkManager

- `RuntimeChunk.zombieIds` 已预留，僵尸通过此字段归属到 Chunk
- ChunkManager 三级切换时遍历 `zombieIds`：
  - Preloaded → Loaded：`NavMeshAgent.enabled = true`，注册到 AwarenessSystem
  - Loaded → Preloaded/Unloaded：`NavMeshAgent.enabled = false`，从 AwarenessSystem 注销

##### 3.8 第一步文件清单

| 文件 | 动作 | 说明 |
|------|:----:|------|
| `Systems/Zombie/ZombieState.cs` | 新建 | 抽象基类 |
| `Systems/Zombie/ZombieStateMachine.cs` | 新建 | 状态机上下文 + 状态切换 |
| `Systems/Zombie/ZombieStates/ZombieState_Idle.cs` | 新建 | 待机（静态 Instance） |
| `Systems/Zombie/ZombieStates/ZombieState_Wander.cs` | 新建 | 漫游（静态 Instance） |
| `Systems/Zombie/ZombieStates/ZombieState_Chase.cs` | 新建 | 追击（静态 Instance） |
| `Systems/Zombie/ZombieStates/ZombieState_Attack.cs` | 新建 | 攻击（静态 Instance） |
| `Systems/Zombie/ZombieStates/ZombieState_Dead.cs` | 新建 | 死亡（静态 Instance） |
| `Systems/Zombie/ZombieAwarenessSystem.cs` | 新建 | 集中式玩家检测 |
| `Systems/Zombie/ZombieController.cs` | 改造 | 删除旧移动逻辑，挂载新组件 |
| `Systems/Combat/DamageableZombie.cs` | 改造 | 死亡时通知状态机进入 Dead |
| `Systems/WorldGen/ChunkManager.cs` | 改造 | 三级切换时激活/休眠僵尸 |
| `Systems/WorldGen/RuntimeChunk.cs` | 无改动 | zombieIds 已预留 |
| `Systems/WorldGen/Stages/SpawnStage.cs` | 无改动 | Enabled 保持 false，第二步再用 |

##### 3.9 第一步验收标准

| 验收项 | 标准 |
|--------|------|
| 僵尸漫游 | 僵尸在 NavMesh 上随机走动，到达后停顿再走 |
| 发现玩家 | 玩家进入 10m → 僵尸切 Chase，全速追来 |
| 追击+攻击 | 追到 1.5m 停步，每 1.5s 造成一次伤害 |
| 失去目标 | 玩家跑出 30m → 僵尸切回 Wander |
| 僵尸死亡 | HP ≤ 0 → Dead → 1s 后 Destroy |
| 集中检测 | AwarenessSystem 统一管理，不分散到各 Update |
| Chunk 联动 | Unloaded Chunk 中僵尸休眠（NavMeshAgent 禁用） |
| 建造阻挡 | 放置建筑自动阻挡僵尸路径（blocksNavMesh 生效） |

##### 3.10 性能分析（以 250 只活跃僵尸为例）

| 开销项 | 频率 | 每帧成本 | 说明 |
|--------|------|---------|------|
| `Update()` 虚方法调用 | 每帧 ×250 | 极轻 | Idle/Wander/Dead 几乎什么都不做 |
| `SetDestination` | 每 0.5s ×追击中僵尸 | 假设 20 只追击 | **节流后开销可忽略** |
| `AwarenessSystem` 遍历 | 每 0.2s ×活跃数 | 1250 次距离/秒 | 比每僵尸自行检测少 12 倍 |
| Chunk 外僵尸 | 0 | 0 | **最大收益：75% 僵尸处于休眠** |

---

#### 第二步：扩展（后续做）

##### 4.1 `ZombieData` ScriptableObject

定义僵尸类型模板：血量/速度/伤害、检测范围、感知权重、体型(半径+高度)、外观索引、Loot 组。

##### 4.2 `ZombieSpawner` + `SpawnStage` 实现

- 按 Chunk 区域类型决定刷什么僵尸
- 按时间：白天少，夜晚多
- 按密度上限：单 Chunk 僵尸数量上限
- 写入 `RuntimeChunk.zombieIds`

##### 4.3 高级感知系统

- 视觉锥：`Vector3.Angle` 判定是否在僵尸前方锥形内，白天远（15m）、夜晚近（5m）
- 听觉事件：新增 `NoiseEvent`，玩家跑步/开枪/敲击 → AwarenessSystem 广播
- 环境遮蔽：`Physics.Linecast` 检测视线是否被墙壁阻挡

##### 4.4 群体行为

- 僵尸发现玩家时"传染"给 10m 内其他僵尸（Wander 状态的跟着切 Chase）
- 小型尸群自然形成

---

### 五、依赖确认

```
ZombieState（无依赖）
  └─ ZombieStateMachine（依赖 ZombieState）
       ├─ 5 个状态实现（依赖 ZombieStateMachine）
       ├─ ZombieAwarenessSystem（依赖 ZombieStateMachine 列表）
       ├─ ZombieController 改造（依赖 ZombieStateMachine + AwarenessSystem）
       └─ ChunkManager 集成（依赖 ZombieStateMachine + AwarenessSystem）

外部依赖（全部已就绪）:
  ✅ ChunkManager Phase1（三级区块 + RuntimeChunk.zombieIds）
  ✅ NavMeshAreas.asset（Walkable / Not Walkable / Jump）
  ✅ BuildableData.blocksNavMesh（7 个建造资产全部 true）
  ✅ GameConstants 僵尸参数（速度/范围/伤害/血量）
  ✅ GameEvents.ZombieDied
  ✅ DamageableZombie（仅需小改造通知状态机）
  ✅ ZombieStates/ 目录已创建
```

---

> **Git 提交**: `僵尸AI(L4): Phase1 完成 — FSM五状态+NavMeshAgent+集中感知+Chunk联动+静态单例共享`

---

### 4.1 后置定稿总结（开发完成后回顾）

#### 计划 vs 实际

| 对比维度 | 前置计划 | 实际完成 | 差异 |
|----------|---------|---------|:--:|
| ZombieState 基类 | 抽象类 Enter/Update/Exit | 完全按计划实现 | ✅ |
| 五个状态 | Idle/Wander/Chase/Attack/Dead | 完全按计划实现，全部静态单例 | ✅ |
| 状态机 ZombieStateMachine | ctx 存数据，状态存逻辑 | 完全按计划实现 | ✅ |
| 集中玩家检测 | ZombieAwarenessSystem 每 0.2s 遍历 | 完全按计划实现 | ✅ |
| NavMeshAgent 迁移 | 替换旧直移逻辑 | 完全按计划实现 | ✅ |
| ZombieController 改造 | 薄壳配置组件 | RequireComponent 三件套自动挂载 | ✅ |
| SetDestination 节流 | 每 0.5s 更新 | 完全按计划实现 | ✅ |
| Chunk 绑定 | RuntimeChunk.zombieInstances | 直接存引用而非 ID，运行时更高效 | 🔄 细节调整 |
| ChunkManager 集成 | Activate/Deactivate + 三级切换联动 | 完全按计划实现，Initialize() 因顺序问题移到 Awake | 🔄 细节调整 |
| DamageableZombie 联动 | Die() 通知状态机 | 完全按计划实现 | ✅ |

#### 新建文件（实际）

| 文件 | 说明 |
|------|------|
| `Systems/Zombie/ZombieState.cs` | 状态抽象基类 |
| `Systems/Zombie/ZombieStateMachine.cs` | 状态机组件，存运行时数据，管理状态切换 |
| `Systems/Zombie/ZombieAwarenessSystem.cs` | 集中式玩家检测单例 |
| `Systems/Zombie/ZombieStates/ZombieState_Idle.cs` | Idle: 站1~4s → Wander / 发现玩家 → Chase |
| `Systems/Zombie/ZombieStates/ZombieState_Wander.cs` | Wander: NavMesh随机点半速漫游 → Idle / 发现玩家 → Chase |
| `Systems/Zombie/ZombieStates/ZombieState_Chase.cs` | Chase: 全速追，每0.5s更新destination → Attack / 丢失 → Wander |
| `Systems/Zombie/ZombieStates/ZombieState_Attack.cs` | Attack: 停步+面朝玩家+cooldown伤害循环 → Chase / 死亡 → Dead |
| `Systems/Zombie/ZombieStates/ZombieState_Dead.cs` | Dead: 禁用agent+碰撞，等Destroy |

#### 改造文件（实际）

| 文件 | 改造内容 |
|------|---------|
| `ZombieController.cs` | 删除旧直接移动逻辑，RequireComponent 自动挂载三件套 |
| `DamageableZombie.cs` | Die() 中新增 `stateMachine.TransitionTo(ZombieState_Dead.Instance)` |
| `ChunkManager.cs` | +RegisterZombie / ActivateZombiesInChunk / DeactivateZombiesInChunk；Initialize() 移到 Awake |
| `RuntimeChunk.cs` | +`zombieInstances: List<ZombieStateMachine>` |

#### 经验教训

- **命名空间冲突**：`_Game.Systems.Time` 命名空间遮蔽了 `UnityEngine.Time`，导致 `Time.deltaTime` 编译报错 CS0234。在 `_Game.Systems.*` 下的文件必须用全限定 `UnityEngine.Time.deltaTime`。
- **执行顺序陷阱**：ChunkManager.Initialize() 最初在 Start()，但其他系统的 Start() 可能先跑，导致 `_chunks` 为 null 引发 NullReferenceException。移到 Awake() 解决了所有初始化顺序问题。
- **状态单例模式收益**：5 个全局 `Instance` 替代每僵尸 5 个 new 对象。100 只僵尸从 500 个状态对象降为 5 个，零 GC 分配。
- **Chunk 休眠才是最大的性能优化**：250 只僵尸中约 75% 处于 Preloaded/Unloaded 状态完全不跑 Update，比所有 Update 内部的微观优化加起来还重要。

---

### 4.1.5 声音系统（DecibelSystem · Phase1 完成 ✅）

> **日期**: 2026-05-20 · **状态**: ✅ Phase1 完成 · **Git**: `声音系统(L4): Phase1 完成` · **依赖**: ChunkManager（已完成）

---

## 一、设计目标

所有能发出声音的动作/人物/僵尸/动物都通过统一入口 `DecibelSystem.Emit()` 发声。僵尸监听非同类+非环境的声音，通过 Chunk 空间分区优化性能。持续声音（引擎/发电机）作为锚点或移动源吸附僵尸。

---

## 二、两级标签

```csharp
[Flags] enum SoundSource   // 谁发出的
{
    None = 0, Player = 1<<0, Zombie = 1<<1, Animal = 1<<2,
    Vehicle = 1<<3, Environment = 1<<4
}

[Flags] enum SoundTag      // 什么类型
{
    None = 0, Footstep = 1<<0, Combat = 1<<1, Gunshot = 1<<2,
    Building = 1<<3, Voice = 1<<4, Mechanical = 1<<5, Impact = 1<<6
}
```

```csharp
public readonly struct NoiseEvent
{
    public Vector3 Position { get; }        // 声音原点（不是物体位置）
    public float Radius { get; }
    public SoundSource Source { get; }
    public SoundTag Tag { get; }
    public GameObject SourceObject { get; } // 发声物体引用（可选）
    public bool IsContinuous { get; }       // 是否持续声音
}
```

---

## 三、DecibelSystem 完整处理流程

```
Emit(position, radius, source, tag, sourceObject, isContinuous)
  │
  ├─ 节流 1: 同源冷却
  │   同一 sourceObject + 同一 Tag → N秒内重复的跳过
  │
  ├─ 节流 2: 同位置合并
  │   同一位置附近已发过更大声音且在冷却期内 → 跳过
  │
  ├─ Modifier 管道
  │   注册的 ISoundModifier 依次处理（天气衰减/封闭空间衰减等）
  │
  ├─ 空间分区
  │   计算受影响 Chunk → 收集 RuntimeChunk.zombieInstances → 精准距离判定
  │
  └─ 通知僵尸: zombie.OnSoundHeard(noise)
       └─ 节流 3: 僵尸端冷却 + 大声音切换
```

### 持续声音接口

```csharp
// 固定持续: 发电机、警报器
StartContinuous(key, position, radius, source, tag);  // 每 N 秒自动 Emit
StopContinuous(key);

// 移动持续: 车辆引擎、奔跑玩家
UpdateContinuousPosition(key, newPosition);  // 更新锚点位置，下次 Emit 用新位置
```

---

## 四、节流机制

| 节流 | 位置 | 逻辑 |
|------|------|------|
| 同源冷却 | DecibelSystem | 同对象+同Tag，N秒内只生效一次 |
| 同位置合并 | DecibelSystem | 近处已有更大声音在冷却期 → 忽略小声音 |
| 僵尸冷却 | ZombieStateMachine | 听到声音后 2s 内忽略更小声音；更大的声音才切目标 |

---

## 五、僵尸声音处理

```
OnSoundHeard(noise):
  ├─ 忽略 Source = Zombie | Environment
  ├─ 对比当前"正在追踪的声音"
  │     新声音.Radius > 旧声音.Radius → 换目标
  │     否则 → 忽略（被更大的声音持续吸引中）
  ├─ 设置 lastHeardPosition = noise.Position（原点）
  └─ 切 Chase，目的地 = 声音原点
```

### 调查行为（僵尸 AI Phase2 实现）

```
Chase 到声音原点:
  有可攻击目标 → Attack
  没有目标 → 附近搜索一圈 → 放弃 → 回到 Idle/Wander
// 持续声音的物体不会被"到达"，僵尸会在原点附近徘徊
```

---

## 六、持续声音

| 类型 | 特点 | 例子 | 行为 |
|------|------|------|------|
| 固定持续 | 位置不变 | 发电机、警报器 | 静止锚点，持续吸附区域内僵尸 |
| 移动持续 | 位置随物体变化 | 车辆引擎、奔跑者 | 每N秒更新原点，僵尸跟着移动 |

只有更大的声音才能把僵尸从持续声源上拉走。

---

## 七、声音穿透（二期实现）

```
封闭空间衰减:
  发声方在封闭空间 → Radius × (1 - 封闭度 × maxAttenuation)
  接收方（僵尸）在封闭空间 → 所有传入声音 × (1 - 封闭度 × maxAttenuation)

封闭度: 0.0 (完全开放) ~ 1.0 (完全密封)
衰减随封闭度线性增加，最大衰减时内外几乎听不见
```

实现为 `ISoundModifier` 的一个实例，不改 DecibelSystem。

---

## 八、视觉+听觉双通道

```
僵尸感知体系:
  ├─ 视觉: ZombieAwarenessSystem 每 0.5s 距离检测（辅助通道）
  └─ 听觉: DecibelSystem → zombie.OnSoundHeard()（主要通道，空间分区）
```

---

## 九、空间分区

```
枪声 50m → 覆盖约 4 个 Chunk (80m) → 检查约 30 只僵尸
脚步声 5m → 覆盖 1 个 Chunk → 检查约 3 只僵尸

100 只活跃僵尸中，单次声音平均只查 3~30 只
利用 ChunkManager 已有的 RuntimeChunk.zombieInstances，不建新索引
```

---

## 十、扩展接口

```csharp
interface ISoundModifier  { NoiseEvent Modify(NoiseEvent original); }  // 天气/室内/穿透
interface ISoundListener { void OnSoundHeard(NoiseEvent noise); }      // 动物AI/NPC
```

两个接口已实现，DecibelSystem 的 Register/Unregister 已就绪。

---

## 十一、声音源数据表

### 瞬时声音

| 系统 | 触发条件 | 半径 | Source | Tag | 优先级 |
|------|---------|:---:|--------|-----|:------:|
| WeaponShooting (手枪) | 每次开火 | 50m | Player | Gunshot | ⭐⭐⭐ |
| WeaponShooting (步枪) | 每次开火 | 80m | Player | Gunshot | ⭐⭐⭐ |
| PlayerCombat (近战命中) | 命中时 | 15m | Player | Combat | ⭐⭐⭐ |
| PlayerCombat (近战挥空) | 每次攻击 | 5m | Player | Combat | ⭐⭐ |
| BuildModeController (放置) | 放置成功 | 20m | Player | Building | ⭐⭐⭐ |
| BuildModeController (拆除) | 拆除成功 | 25m | Player | Building | ⭐⭐⭐ |
| VehicleController (碰撞) | 碰撞时 | 40m | Vehicle | Impact | ⭐⭐⭐ |
| WorldContainer (打破门窗) | 破坏时 | 30m | Player | Impact | 二期 |

### 持续声音

| 系统 | 触发条件 | 半径 | Source | Tag | 类型 |
|------|---------|:---:|--------|-----|------|
| PlayerController (跑步) | 跑步中每 0.3s | 8m | Player | Footstep | 移动持续 |
| PlayerController (走路) | 走路中每 0.5s | 3m | Player | Footstep | 移动持续 |
| VehicleController (引擎) | 启动→熄火 | 30m | Vehicle | Mechanical | 移动持续 |
| 僵尸呻吟 | 僵尸存活中 | 15m | Zombie | Voice | 固定持续 |

---

## 十二、声源接入顺序

```
第一步（验证闭环 · 瞬时声音）:
  ① WeaponShooting — 枪声最大，效果最明显
  ② PlayerCombat — 近战命中

第二步（验证持续声音）:
  ③ PlayerController — 跑步/走路持续

第三步（持续+移动）:
  ④ VehicleController — 引擎持续+位置更新

第四步（填充 · 僵尸 Phase2 时）:
  ⑤ BuildModeController — 建造/拆除
  ⑥ 僵尸自身呻吟
```

---

## 十三、三层架构

```
┌──────────────────────────────────────────────────────────┐
│                                                          │
│  玩家开枪      玩家跑步      车辆引擎      僵尸呻吟       │
│     │            │            │            │             │
│     ▼            ▼            ▼            ▼             │
│  ┌─────────────────────────────────────────────┐        │
│  │           SoundEmitter（声源层）              │        │
│  │    参数集中 · 单一切入 · 未来可接音频播放     │        │
│  └────────────────────┬────────────────────────┘        │
│                       │  Emit() / StartContinuous()      │
│                       ▼                                  │
│  ┌─────────────────────────────────────────────┐        │
│  │          DecibelSystem（声音处理层）          │        │
│  │  节流1+2 → Modifier管道 → 空间分区 → 分发     │        │
│  └────────┬──────────────────┬─────────────────┘        │
│           │                  │                           │
│           ▼                  ▼                           │
│  ┌──────────────┐   ┌──────────────────┐                │
│  │  僵尸AI消费   │   │  其他监听者消费    │                │
│  │  过滤→追击   │   │  ISoundListener   │                │
│  └──────────────┘   └──────────────────┘                │
│                                                          │
│  未来扩展（不动现有代码）:                                  │
│    AudioManager   ← 声源层加播放给玩家听                   │
│    封闭空间Modifier ← 室内声音衰减                         │
│    天气Modifier    ← 雨天声音衰减                          │
└──────────────────────────────────────────────────────────┘
```

| 层 | 职责 | 知不知道上层/下层 |
|------|------|:--:|
| **SoundEmitter** | 定义什么动作配什么声音参数，零监听 | 调用 DecibelSystem |
| **DecibelSystem** | 节流 + 处理 + 分发 | 不知道消费者是谁 |
| **僵尸/动物/NPC** | 听到声音后自己的行为逻辑 | 不知道声音从哪来 |

### SoundEmitter 设计

```csharp
public static class SoundEmitter
{
    // 战斗
    public static void EmitGunshot(Vector3 pos, bool isRifle = false);
    public static void EmitMeleeHit(Vector3 pos);
    // 移动（持续）
    public static void SetFootstep(string key, Vector3 pos, bool running);
    public static void StopFootstep(string key);
    // 建造
    public static void EmitBuildPlace(Vector3 pos);
    public static void EmitBuildDeconstruct(Vector3 pos);
    // 车辆
    public static void StartVehicleEngine(string key, Vector3 pos);
    public static void UpdateVehicleEngine(string key, Vector3 pos);
    public static void StopVehicleEngine(string key);
}
```

参数全在 SoundEmitter 内部，各系统调一个方法即可，不知道 DecibelSystem 存在。

---

## 十四、文件清单

### 已接入声源（全部完成 ✅）

| 文件 | 说明 |
|------|------|
| `Systems/Audio/DecibelSystem.cs` | 单例 + Emit + 节流1+2 + 空间分区 + 持续声音 + Modifier/Listener |
| `Systems/Audio/ISoundListener.cs` | 监听者接口 |
| `Systems/Audio/ISoundModifier.cs` | 修改器接口 |
| `Systems/Audio/SoundEmitter.cs` | 声源静态类，参数集中 |
| `Core/GameEvents.cs` | +SoundSource +SoundTag +NoiseEvent |
| `Systems/Zombie/ZombieStateMachine.cs` | +lastHeardPosition +OnSoundHeard(过滤+节流3) |
| `Systems/Zombie/ZombieStates/ZombieState_Chase.cs` | 支持去声音原点调查 |
| `Systems/Zombie/ZombieAwarenessSystem.cs` | 视觉检测降频到 0.5s |
| `Systems/Weapon/WeaponShooting.cs` | 接入: 开火→SoundEmitter.EmitGunshot ✅ |
| `Systems/Combat/PlayerCombat.cs` | 接入: 命中→EmitMeleeHit / 挥空→EmitMeleeSwing ✅ |
| `Systems/Player/PlayerController.cs` | 接入: 跑步/走路→SetFootstep持续声音 ✅ |

### 待接入声源（后续逐步接入）

| 系统 | 方法 | 说明 |
|------|------|------|
| `BuildModeController` | EmitBuildPlace / EmitBuildDeconstruct | 建造/拆除声音 |
| `VehicleController` | StartVehicleEngine / UpdateVehicleEngine / StopVehicleEngine | 车辆引擎持续声音 |
| `ZombieStateMachine` | EmitZombieGroan / StopZombieGroan | 僵尸呻吟持续声音 |

---

## 十五、扩展场景验证

| 未来需求 | 做什么 | 改 DecibelSystem？ |
|----------|--------|:--:|
| 加新声源类型 NPC | 枚举加 `NPC = 1 << 5` | 一行 |
| 加新声音类型爆炸 | 枚举加 `Explosion = 1 << 7` | 一行 |
| 僵尸只听枪声 | OnSoundHeard 加 Tag 过滤 | 否 |
| 动物 AI 听声音逃跑 | 实现 ISoundListener，Register | 否 |
| 雨天声音衰减 | WeatherModifier : ISoundModifier，Register | 否 |
| 封闭空间衰减 | IndoorModifier : ISoundModifier，Register | 否 |
| 所有系统接入声源 | 各自加 1~3 行 Emit | 否 |

---

### 4.1.5 后置定稿总结（开发完成后回顾）

#### 计划 vs 实际

| 对比维度 | 前置计划 | 实际完成 | 差异 |
|----------|---------|---------|:--:|
| DecibelSystem 核心 | Emit + 节流1+2 + 空间分区 + 持续声音 | 完全按计划实现 | ✅ |
| 两级标签 | SoundSource + SoundTag Flags 枚举 | 完全按计划实现 | ✅ |
| 三层架构 | SoundEmitter → DecibelSystem → Consumer | 完全按计划实现 | ✅ |
| 僵尸听觉 | OnSoundHeard 过滤+节流3+大声音切换 | 完全按计划实现，追加声音原点调查行为 | ➕ |
| Chase 调查 | Phase2 才做 | 提前实现：无玩家目标时追踪声音原点，到达→Idle | ➕ |
| 声源接入 | WeaponShooting / PlayerCombat / PlayerController | 全部完成 | ✅ |
| 建造/车辆声源 | 待定 | SoundEmitter 方法已预留，待后续接入 | ⏳ |
| 封闭空间穿透 | Phase2 | 未开始（ISoundModifier 接口已就绪） | ⏳ |
| 调试 UI | 分散在各系统的 OnGUI（重叠混乱） | 统一 DebugPanel（Y 切换）+ DecibelHUD（始终可见） | 🔄 升级 |
| 噪声等级 | 无设计 | GetAmbientNoiseLevel 6级梯度归一化算法 | ➕ |

#### 新建文件（实际）

| 文件 | 说明 |
|------|------|
| `Systems/Audio/DecibelSystem.cs` | 单例，Emit + 节流1+2 + Modifier管道 + 空间分区 + 持续声音管理 + GetAmbientNoiseLevel |
| `Systems/Audio/ISoundListener.cs` | 监听者接口（NPC/动物AI消费） |
| `Systems/Audio/ISoundModifier.cs` | 修改器接口（天气/封闭空间衰减） |
| `Systems/Audio/SoundEmitter.cs` | 静态声源层，参数集中，所有发声系统调这里 |
| `UI/DecibelHUD.cs` | 左上角噪声6级显示（安静→震耳欲聋），驾驶时自动下移 |
| `UI/DebugPanel.cs` | 按 Y 切换开发者面板，整合 Chunks/Zombies/Decibel 三区 |
| `Editor/SetupDebugUI.cs` | 菜单工具一键创建 DecibelHUD + DebugPanel |

#### 改造文件（实际）

| 文件 | 改造内容 |
|------|---------|
| `Core/GameEvents.cs` | +SoundSource/SoundTag 枚举 +NoiseEvent 结构体 |
| `Core/GameConstants.cs` | +MAX_SOUND_RADIUS = 80f |
| `ZombieStateMachine.cs` | +lastHeardPosition +OnSoundHeard(过滤僵尸/环境+节流3+大声音切换) |
| `ZombieState_Chase.cs` | 支持无玩家目标时去声音原点调查(到达2m→Idle) |
| `ZombieAwarenessSystem.cs` | 视觉检测降频到 0.5s（听觉已成为主通道）；删 OnGUI，+ActiveZombieCount |
| `WeaponShooting.cs` | 开火→SoundEmitter.EmitGunshot(muzzle) |
| `PlayerCombat.cs` | 命中→EmitMeleeHit / 挥空→EmitMeleeSwing |
| `PlayerController.cs` | 跑步/走路→SetFootstep 持续声音 + OnDestroy 清理 |
| `ChunkManager.cs` | 删 OnGUI，+PreloadQueueCount +LastPlayerSpeed +GetChunkCounts() |
| `DecibelSystem.cs` | 删 OnGUI，+公开属性 +GetAmbientNoiseLevel |

#### 经验教训

- **字典遍历安全**：Unity Mono 运行时的 `Dictionary` 在 `foreach` 时即使只通过索引器更新已有 key 也会抛 `InvalidOperationException`。必须先收集再改字典。
- **噪声等级校准**：直接 `(1 - dist/radius)` 会导致玩家自己走路=100%噪声（因为距离为0）。必须加入声音类型的绝对响度：`(radius/MAX) × (1 - dist/radius)` 才能正确区分走路(4%)和枪声(63%~100%)。
- **OnGUI 散落问题**：多个系统各自画 OnGUI 导致位置重叠。提前设计统一的调试面板（Y 切换）比事后清理更高效。
- **声源层价值**：SoundEmitter 静态类让 WeaponShooting/PlayerCombat 等系统零感知 DecibelSystem 存在，未来加 AudioManager 播放声音给玩家也只需改 SoundEmitter 一处。

---

### 4.1 第二步计划（Phase2 · ✅ 已完成）

> **日期**: 2026-05-20 · **状态**: ✅ Phase2 完成 · **Git**: d06f988 · **依赖**: ChunkManager + 声音系统 Phase1（全部已就绪）

---

### 4.1 Phase2 后置定稿总结（开发完成后回顾）

#### 计划 vs 实际

| 对比维度 | 前置计划 | 实际完成 | 差异 |
|----------|---------|---------|:--:|
| ZombieData SO | 僵尸类型模板，3 默认资产 | 完全按计划实现 | ✅ |
| ZombieStateMachine 改造 | ZombieData 替代硬编码 | +ApplyZombieData()，+防重复注册 | ✅ |
| ZombieController 改造 | 薄壳引用 ZombieData | 同步设置 DamageableZombie.maxHealth | ➕ |
| ZombieSpawner | 按区域+时间+密度刷怪 | 两阶段预算制（初始+持续补刷）+ 隐身刷新 + 权重随机 | 🔄 升级 |
| ZoneSpawnProfile | 未在原始计划中 | 新增 SO：地段参数集中管理，后续多地段差异化零代码 | ➕ |
| SpawnStage | 填充 Execute 挂入管线 | 保持 Enabled=false，运行时由 ChunkManager.Stage2 驱动 | 🔄 架构调整 |
| 视觉锥 | ZombieData.visionAngle 锥形检测 | 完全按计划实现，visionAngle=0 回退圆形 | ✅ |
| 听觉联动 | 已在声音系统 Phase1 完成 | 零改动，直接生效 | ✅ |
| 视线遮蔽 | Physics.Linecast 阻挡判定 | 完全按计划实现 | ✅ |
| 群体行为 | 15m 内连锁唤醒 | 完全按计划实现（逐帧级联，涟漪扩散） | ✅ |
| Chunk 卸载 | 僵尸休眠保留 | 改为销毁僵尸 + 重置预算，下次加载全新刷新 | 🔄 架构调整 |
| 昼夜倍率 | 夜晚密度 ×2 | 完全按计划实现（Spawner.nightMultiplier + IsNight 判定） | ✅ |
| 地段区分 | 按区域类型差异配置 | ZoneSpawnProfile + 伪随机哈希分配，后续替换为真实区域数据 | ⏳ 基础就绪 |

#### 新建文件（实际）

| 文件 | 说明 |
|------|------|
| `Config/ZombieData.cs` | 僵尸类型 SO 模板：血量/速度/伤害/感知/视野锥/体型(半径+高度)/掉落 |
| `Config/ZoneSpawnProfile.cs` | 地段刷怪配置 SO：数量/间隔/隐身距离/类型权重 |
| `Systems/Zombie/ZombieSpawner.cs` | 两阶段刷怪系统：PhaseA 初始刷怪 + PhaseB 持续补刷（预算制+隐身刷新+权重随机+昼夜倍率+BuildBody圆柱体） |
| `Editor/CreateDefaultZombieData.cs` | 一键创建 3 种 ZombieData(含体型) + 默认 ZoneSpawnProfile + Spawner 场景对象 |
| `Editor/SetupZombieSpawnDebug.cs` | 一键创建 F1 调试面板 GameObject |
| `UI/ZombieSpawnDebugWindow.cs` | F1 开发者刷怪面板：距离/类型/数量可调，圆柱体可视化 |

#### 改造文件（实际）

| 文件 | 改造内容 |
|------|---------|
| `ZombieStateMachine.cs` | +ApplyZombieData() 从 SO 批量应用参数；Start() 防重复注册（Contains 检查） |
| `ZombieController.cs` | +zombieData 引用替代硬编码字段；+Apply() 提取共用逻辑；+Initialize() 修复 AddComponent 后数据覆盖（Spawner 刷怪此前一直用默认值） |
| `ZombieSpawner.cs` | SpawnZombie 改调 Initialize()；+BuildBody() 静态工具方法创建圆柱体+缩放 Collider |
| `ZombieAwarenessSystem.cs` | 完全重写：+CanSeePlayer（视觉锥+Linecast遮蔽）+CascadeToNearby（逐帧级联扩散）+_newlyDetected 列表 |
| `ChunkManager.cs` | Stage2 调 SpawnInitial；EnterUnloaded 调 DestroyZombiesInChunk + OnChunkUnloaded；+IsNightTime()；+DestroyZombiesInChunk() |
| `SpawnStage.cs` | 更新注释（运行时由 ChunkManager 驱动） |
| `UI/DebugPanel.cs` | +ZombieSpawner 当前 Chunk 存活/预算显示 |
| `UI/ZombieSpawnDebugWindow.cs` | 新建：F1 调试面板，距离/类型/数量可调配刷怪 |
| `Editor/SetupZombieSpawnDebug.cs` | 新建：一键创建 F1 面板 GameObject |
| `Core/GameEvents.cs` | 无需改动（NoiseEvent 已在声音系统 Phase1 完成） |

#### 刷怪机制详解

```
┌─ Phase A（初始）──────────────────────────────────────┐
│ ChunkManager.Stage2_RespawnNPCs                       │
│   → ZombieSpawner.SpawnInitial(chunkId, isNight)      │
│   → 按 ZoneSpawnProfile 刷 initialMin~initialMax 只    │
│   → 权重随机选 ZombieData 类型                         │
│   → 预算 = maxPerChunk - 实际刷出                      │
└──────────────────────────────────────────────────────┘
                         │
                         ▼
┌─ Phase B（持续补刷）──────────────────────────────────┐
│ ZombieSpawner.Update() 每 10s                         │
│   → 遍历 _alivePerChunk，找 Loaded 状态的 Chunk        │
│   → 冷却检查（respawnInterval 每 Chunk）               │
│   → 当前存活 < maxPerChunk？补刷 1~2 只                │
│   → 隐身刷新：NavMesh找点 + 距玩家 > 25m               │
│               + dot(player.forward, toSpawn) < 0.3     │
└──────────────────────────────────────────────────────┘
                         │
         ┌───────────────┼───────────────┐
         ▼               ▼               ▼
    僵尸死亡          Chunk 卸载       玩家远离
  预算 +1          销毁全部僵尸      不再补刷
  下次补刷填补      预算全清重置      Chunk休眠
```

#### 3 种默认僵尸

| 类型 | 血量 | 速度 | 伤害 | 视野锥 | 权重 |
|------|:---:|:---:|:---:|:-----:|:---:|
| 普通僵尸 | 100 | 3 | 10 | 90° | 60 |
| 快速僵尸 | 50 | 6 | 8 | 120° | 25 |
| 胖僵尸 | 300 | 1.5 | 20 | 60° | 15 |

#### 经验教训

- **预算制优于存活计数**：不直接统计每 Chunk 当前僵尸数，改用 `maxPerChunk - 已刷 + 死亡返还`。避免僵尸跨 Chunk 移动导致的计数错乱。
- **Chunk 卸载必须销毁僵尸**：旧设计让僵尸休眠保留（SetActive=false），但刷怪预算重置后会重复刷。改为卸载时 Destroy + Clear，下次加载全新初始化，逻辑更清晰。
- **僵尸类型配置移到 Profile**：Spawner 本身不直接持有 ZombieData 列表，而是通过 ZoneSpawnProfile.typeWeights 间接引用。加新地段只需创建新 Profile，不碰 Spawner 代码。
- **隐身刷新的三个条件**：距离(25m) + 视野(dot<0.3) + NavMesh 有效点，三重保障玩家不会看到僵尸凭空出现。实践中 10 次寻点重试足够找到合法位置。

---

#### 4.1.8 背包拖拽系统 ✅（Phase1 完成）

> **日期**: 2026-05-21 · **状态**: ✅ Phase1 完成 · **Git**: 5d0cdbf · **依赖**: 背包系统 + InventoryUI

**涉及文件**: `PlacedItem.cs` / `InventoryContainer.cs` / `DragDropManager.cs` / `ItemDragHandler.cs` / `InventoryUI.cs`

##### 功能详情

**PlacedItem 扩展**:
- 新增 `rotated` 字段(bool)，标记物品是否旋转90°
- `GridWidth`/`GridHeight` 根据旋转状态自动交换
- `OriginalWidth`/`OriginalHeight` 保留原始尺寸

**坐标检测拖拽 (DragDropManager)**:
- `CellRegion` 结构体: 记录每个格子的 RectTransform + 所属容器 + 网格坐标 + 是否武器槽
- `FindCellAtPosition(screenPos)`: 反向遍历已注册格子，用 `RectTransformUtility.RectangleContainsScreenPoint` 判定落点
- `ComputeDropPosition(cr)`: 根据点击偏移 + 旋转状态计算放置位置

**T键旋转 (3种上下文)**:
- 按住未拖时: 原地旋转 (`RotateInPlace`)，在源容器内更新位置，用 `IsSpaceFreeFor` 排除自身格子
- 拖拽中: toggle `_dragRotated` 标记，落点时校验
- 已选中时: 旋转选中物品 (`RotateSelectedItem`)
- 旋转公式: `(rx, ry) → (ry, rx)`，原点格子不动，矩形绕原点旋转90°

**选中系统**:
- 点击物品立刻显示白框，长按持续存在
- 白框实现: 在 overlay 上添加 4 条白色 Image (SelTop/SelBot/SelLeft/SelRight)，不触发 UI 重建
- `RefreshSelectionBorder()`: UI 重建后通过已注册格子查找 overlay 恢复白框

**放置逻辑 (TryMoveToContainer)**:
- 先检测是否为武器槽自动装备
- 同容器旋转检测用 `IsSpaceFreeFor`（排除自身格子）
- 不同容器直接 `IsSpaceFree`
- 放不下时回退到原位，弹出 Toast

---

#### 4.1.9 武器切换 + 腰带检测 + 装备去重 ✅（Phase1 完成）

> **日期**: 2026-05-21 · **状态**: ✅ Phase1 完成 · **Git**: 5d0cdbf · **依赖**: 装备系统 + InventoryContainer

**涉及文件**: `WeaponSwitcher.cs`(新建) / `Inventory.cs` / `InventoryContainer.cs`

##### WeaponSwitcher (新建组件)
- Q键循环切换武器槽: RightHand → LeftHand → KnifeBelt → SidearmBelt
- 切换时自动启用/禁用 PlayerCombat / WeaponShooting
- 滚轮切换支持

##### 装备去重
- `Inventory.EquipItem` 返回 `bool`
- 装备前自动 `UnequipSlot` 防重复
- 支持 `targetSlot` 参数精确指定武器槽

##### 腰带检测
- `HasBeltSpaceFor(itemData)`: 检查腰带容器空闲格
- 扣除同槽旧幽灵格 (KnifeBelt/SidearmBelt 各占2格)
- 空间不足时发布 `belt_full` 事件 → InventoryUI 显示提示

---

#### 4.1.10 角色面板 ✅（Phase1 完成）

> **日期**: 2026-05-21 · **状态**: ✅ Phase1 完成 · **Git**: 62653a0 · **依赖**: SurvivalXPSystem + InventoryUI

**涉及文件**: `SurvivalXPSystem.cs` / `InventoryUI.cs`

##### SurvivalXPSystem 改造
- `AddXP(int amount)`: 改为**仅累积经验**，不再自动兑换技能点
- `ConvertXPToPoints()`: 新增手动兑换方法，按 `XP_PER_SKILL_POINT(100)` 兑换，返回实际获得点数，发布 `CharacterStatsChanged("xp_converted")`

##### 角色面板布局 (ShowCharacterTabContent)
- **左面板 38%**: 职业名 → XP进度条(当前/100) → "兑换技能点"按钮(灰色不足/绿色可兑) → 可用技能点数(绿色高亮) → 分隔线 → 4属性(力敏体耐)各有进度条(0-10) + 升级按钮(显示耗点)
- **右面板 62%**: 10技能分4组(战斗/生存/制造/心智)，每组标题分隔，每行: 技能名 | Lv.进度条(颜色填充0-10) | 升级按钮(显示耗点或MAX)
- **Tab切换**: 装备容器/角色均保持 HUD 隐藏

##### 自动刷新
- 订阅 `CharacterStatsChanged` 事件
- 角色面板打开时自动响应升级事件刷新

##### FlashGreen 升级动画
- `FlashGreen(RectTransform target, Action onComplete)`: 将目标 Image 闪绿色 0.15s 后恢复
- **Bug修复**: 第一版 `ShowOverview()` 在协程运行中直接销毁 UI，导致 `MissingReferenceException`。改为 onComplete 回调模式，闪绿结束后才调用 `ShowOverview()`，协程内加 Image null 检查

##### 调试面板增强 (F2)
- F2 键 (Y键继续有效) 切换面板
- XP 按钮: +50 / +100 / +500
- 技能点按钮: 兑换XP→点数 / +1技能点 / +5技能点
- 每个技能右侧 ↑ 按钮，点击直接 `SpendPoint`

---

#### 4.2 配方/制作 📋（前置计划已定稿）


##### 核心底层规则

###### 物品大类划分（全品类闭环）

| 大类 | 内容 | 来源 |
|------|------|------|
| 原材料 | 木头、石头、草药、矿石、兽皮、杂草、废水 | 野外采集/搜刮 |
| 半成品 | 木板、铁锭、布料、火药、零件 | 原材料加工 |
| 消耗品 | 食物、饮水、药品、投掷物、弹药、燃料 | 合成/搜刮 |
| 装备类 | 近战/远程武器、护甲、头盔、工具 | 合成/搜刮 |
| 建筑类 | 墙体、地板、门窗、陷阱、储物 | 建造系统 |
| 功能道具 | 图纸、技能书、钥匙、任务物品 | 搜刮/掉落 |
| 工作台 | 简易/中级/高级工作台、篝火、熔炉 | 合成后放置 |

###### 物品三大核心属性

**品质** (`ItemQuality` 枚举，挂在 `ItemData`)：
```
Scavenged   — 地图刷新/搜刮品（属性最优，不可制造）
Handmade    — 自制（可制造，属性略低于搜刮品）
Professional — 高端自制（高级技能+稀有材料，属性接近搜刮品）
```

**耐久值**：装备/工具独有，归零报废，可维修。`ItemData` 加 `maxDurability`。

**用途标签** (`ItemUsageTag` 枚举，`ItemData` 加 `usageTags[]` 数组)：
```
烹饪 / 冶炼 / 建造 / 战斗 / 医疗 / 能源
```
用于 UI 分页 + 工作台权限匹配（熔炉只认"冶炼"标签材料）。

###### 材料合并原则

同源物料大一统命名，删减细碎分类：
- 木材 = 树枝+原木+枯木
- 草本纤维 = 干草+植物纤维+芦苇+枯叶
- 草药 = 普通+珍稀草药
- 石料 = 碎石+原石+砂石
- 生肉 = 所有野兽肉类
- 碎布料 = 破旧布料+布条
- 废金属 = 生锈金属+边角料
- 通用零件 = 老旧零件+小配件
- 空容器 = 玻璃瓶+塑料瓶

##### 物品层级关联逻辑（上下游链状绑定）

核心规则：**高阶物品必须依赖低阶半成品，不能跳级合成。同材质物品共用基础材料，减少冗余素材。**

标准流转链示例：

| 链条 | 步骤1 | 步骤2 | 步骤3 | 最终成品 |
|------|-------|-------|-------|----------|
| 纺织链 | 植物纤维 | → 麻绳 | → 粗布 → 棉布 | 高级防护布料 |
| 金属链 | 铁矿石 | → 粗铁矿 | → 铁锭 → 铁零件 | 铁质武器/防具 |
| 食物链 | 野果/生肉 | → 篝火烹煮 | → 熟食 | 便携罐头 |
| 药品链 | 草药 | → 研磨提纯 | → 基础药剂 | 高阶buff药剂 |
| 回收链 | 废旧枪械 | → 高级台拆解 | → 枪械零件 | 组装新枪 |

强制关联规则：
- 特殊道具只绑定专属产出途径（图纸解锁、僵尸掉落、宝箱搜刮）
- 半成品统一归类收纳，不拆分过多冷门素材
- 一物尽量只留 1-2 种主流合成路线，不做同功能多材料合成

##### 双门槛权限体系

**门槛1：制作台权限锁**

| 制作方式 | 可合成范围 | 禁止操作 |
|----------|-----------|----------|
| 徒手 | 最基础原材料、简易耗材 | 所有半成品、成品装备 |
| 简易工作台 | 木质物件、初级建材、低级工具 | 金属制品、药品、枪械 |
| 中级工作台 | 石材制品、普通防具、基础近战 | 枪械、炸药、高阶药剂 |
| 高级工作台 | 全品类成品 + 拆解 + 维修 + 改装 | 无法熔炼矿石、无法生火做饭 |
| 熔炉 | 仅熔炼矿石、烧制原料锭 | 不做任何成品装备 |
| 篝火 | 仅烹饪、烧水、食材处理 | 无任何造物能力 |

**门槛2：人物技能等级锁**

台子等级够了，技能不够依旧不能合成：
- 建造技能 → 解锁工作台升级 + 所有建筑配方
- 生存厨艺 → 解锁篝火高阶食谱、长效食物
- 冶炼技能 → 提升熔炉熔炼效率、解锁合金锭
- 医疗技能 → 解锁各级药剂、急救物品合成
- 枪械精通 → 解锁手枪→步枪→狙击全枪械 + 对应子弹
- 机械工艺 → 解锁载具配件、装备改装、精密零件

##### 品质设计决策

品质挂在 `ItemData` 层（物品定义层），不同品质 = 不同 `.asset` 文件：
- `绷带_简易.asset` → quality=Handmade, 回血15, 消轻微出血
- `绷带_军用.asset` → quality=Scavenged, 回血40, 消重度出血+防感染

好处：图标不同、描述不同、属性固定、制作配方直接指定产出哪个，运行时不搞复杂。

##### 材料互通与资源循环

- **碎金属统一**：不分武器拆解还是建筑拆解，统一归类为金属素材
- **装备拆解回流闭环**：淘汰旧装备 → 高级台拆解 → 返还基础材料 → 重新打造新装备
- **食材互通**：所有肉类、野菜可自由搭配烹饪，组合溢出转buff
- **弹药通用基底**：所有子弹共用火药+弹壳基底，仅搭配不同弹头区分类型
- **维修耗材互通**：武器护甲通用维修基础材料，高阶维修仅增加稀有辅料

##### 简化决策备忘

为什么只保留 2 个专属台（篝火 + 熔炉）：
- 通用工作台做全品类整合，砍掉化学台/军械台等重复冗余
- 只保留不可替代、玩法刚需的专属台：篝火（生火烹饪唯一）+ 熔炉（矿石熔炼唯一）
- 拆分逻辑清晰：原料冶炼归熔炉，成品打造归高级工作台
- 高级台能替代多数细分制作，唯独生活刚需专属台不可替代

##### 避坑设计原则

1. 不做同功能多材料合成，一物尽量只留 1-2 种主流合成路线
2. 半成品统一归类收纳，不拆分过多冷门素材
3. 合成界面按物品大类分页：食材、建材、武器、药品、弹药、改装
4. 自动前置提示：缺少材料 / 台子不够 / 技能不足，直接红字提示
5. 批量合成功能，支持一次性制作多个同类物品

###### 1.1 枚举定义

**重写**: `Assets/_Game/Config/ItemCategory.cs` → 8 大类
```csharp
public enum ItemCategory
{
    RawMaterial,   // 原材料（木头/石头/草药/矿石/兽皮/杂草/废水）
    SemiFinished,  // 半成品（木板/铁锭/布料/火药/零件）
    Consumable,    // 消耗品（食物/饮水/药品/投掷物）
    Equipment,     // 装备（武器/护甲/头盔/工具）
    Ammo,          // 弹药
    Buildable,     // 建筑（墙体/地板/门窗/陷阱）
    Workstation,   // 工作台（可放置物件）
    Functional     // 功能道具（图纸/技能书/钥匙/任务物品）
}
```

**新文件**: `Assets/_Game/Config/ItemQuality.cs`
```csharp
public enum ItemQuality { Scavenged, Handmade, Professional }
```

**新文件**: `Assets/_Game/Config/ItemUsageTag.cs`
```csharp
public enum ItemUsageTag { 烹饪, 冶炼, 建造, 战斗, 医疗, 能源 }
```

**新文件**: `Assets/_Game/Config/WorkstationTier.cs`
```csharp
public enum WorkstationTier
{
    Hands,         // 徒手（无门槛）
    SimpleBench,   // 简易工作台
    MediumBench,   // 中级工作台
    AdvancedBench, // 高级工作台
    Furnace,       // 熔炉（专属）
    Campfire       // 篝火（专属）
}
```

###### 1.2 ItemData 扩展

`Assets/_Game/Config/ItemData.cs` 新增字段：
```csharp
[Header("品质 & 耐久")]
public ItemQuality quality = ItemQuality.Scavenged;
public bool hasDurability;
public float maxDurability = 100f;

[Header("用途标签")]
public ItemUsageTag[] usageTags;

[Header("工作台")]
public bool isWorkstation;
public WorkstationTier workstationTier;
```

###### 1.3 RecipeData ScriptableObject

**新文件**: `Assets/_Game/Config/RecipeData.cs`
```csharp
[CreateAssetMenu(menuName = "Game/Recipe")]
public class RecipeData : ScriptableObject
{
    [Header("基础信息")]
    public string recipeName;
    public Sprite icon;
    public string description;
    public RecipeCategory category;

    [Header("合成需求")]
    public WorkstationTier requiredStation;
    public SkillRequirement[] skillRequirements;
    public ItemRequirement[] materials;             // 复用现有 ItemRequirement

    [Header("产出")]
    public ItemData resultItem;
    public int resultCount = 1;

    [Header("合成参数")]
    public float craftTime = 2f;
    public float xpReward = 15f;

    [Header("拆解")]
    public bool canDeconstruct;
    [Range(0, 1)] public float deconstructReturnRate = 0.5f;
}
```

复用: `SkillType`(现有10技能), `ItemRequirement`(现有,在BuildableData.cs)

新增:
```csharp
[Serializable] public struct SkillRequirement { public SkillType skill; public int level; }
public enum RecipeCategory { Tool, Building, Weapon, Armor, Consumable, Ammo, Vehicle, Material, Cooking, Smelting }
```

###### 1.4 RecipeCatalog

**新文件**: `Assets/_Game/Config/RecipeCatalog.cs`
```csharp
[CreateAssetMenu(menuName = "Game/Recipe Catalog")]
public class RecipeCatalog : ScriptableObject
{
    public RecipeData[] recipes;
    public RecipeData[] GetByStation(WorkstationTier tier);
    public RecipeData[] GetByCategory(RecipeCategory cat);
}
```

**新文件**: `Assets/_Game/Editor/CreateRecipeCatalog.cs` — 一键搜集 RecipeData

###### 1.5 CraftingSystem

**新文件**: `Assets/_Game/Systems/Crafting/CraftingSystem.cs`
```csharp
public class CraftingSystem : MonoBehaviour
{
    public static CraftingSystem Instance;
    RecipeCatalog _catalog;
    Inv _inventory;
    public WorkstationTier ActiveStation { get; set; }

    public List<RecipeData> GetAvailableRecipes();
    public bool CanCraft(RecipeData recipe);
    public bool Craft(RecipeData recipe);
    public int GetCraftableCount(RecipeData recipe);
}
```

Craft 流程: `CanCraft → RemoveItem → AddItem → Publish(SurvivalXpGained) → Publish(InventoryChanged) → Publish(CraftingEvent)`

**GameEvents.cs** 新增: `public readonly record struct CraftingEvent(string recipeName, bool success);`

###### 1.6 文件夹重组

```
Config/
  Items/
    RawMaterials/      木头、石料、铁矿、草药、兽皮、碎布料、废金属...
    SemiFinished/      木板、铁锭、布料、火药、通用零件...
    Consumables/       食物、饮水、药品、投掷物...
    Equipment/         近战武器、远程枪械、护甲、头盔、工具...
    Ammo/             手枪弹、步枪弹、霰弹...
    Buildables/        墙体、地板、门窗、陷阱...
    Workstations/      简易工作台、中级工作台、高级工作台、篝火、熔炉...
    Functional/        图纸、技能书、钥匙...
  Recipes/
    徒手/
    简易工作台/
    中级工作台/
    高级工作台/
    篝火/
    熔炉/
```

###### 1.7 默认配方（第一批）

#### 徒手 (Hands)

| 配方 | 材料 | 产出 |
|------|------|------|
| 木棍 | 木材×1 | 木棍×1 |
| 木板 | 木材×2 | 木板×2 |
| 简易绷带 | 碎布料×1 | 绷带(自制)×1 |
| 绳索 | 草本纤维×2 | 绳索×1 |
| 粗布 | 草本纤维×2 + 碎布料×1 | 粗布×1 |
| 简易工作台 | 木板×4 + 通用零件×2 | 简易工作台×1 |

#### 篝火 (Campfire)

| 配方 | 材料 | 产出 |
|------|------|------|
| 净水 | 生水×1 + 木柴燃料×1 | 净水×1 |
| 熟肉 | 生肉×1 + 木柴燃料×1 | 熟肉×1 |
| 野菜餐 | 食用野菜×2 + 木柴燃料×1 | 野菜餐×1 |
| 劣质熟腐肉 | 腐肉×1 + 木柴燃料×1 | 劣质熟腐肉×1（可食用但有debuff风险） |
| 杂烩菜 | 任意食材×N + 木柴燃料×1 | 杂烩菜(饱食度=Σ食材) |

#### 熔炉 (Furnace)

| 配方 | 材料 | 产出 |
|------|------|------|
| 铁锭 | 铁矿×3 + 煤炭×1 | 铁锭×1 |
| 铜锭 | 铜矿×3 + 煤炭×1 | 铜锭×1 |
| 石砖 | 石料×4 | 石砖×2 |

#### 简易工作台 (SimpleBench)

| 配方 | 材料 | 技能 | 产出 |
|------|------|------|------|
| 加厚木板 | 木板×3 | 无 | 加厚木板×1 |
| 木栅栏 | 木板×4 + 绳索×2 | 无 | 木栅栏×1 |
| 小型储物木箱 | 木板×6 + 通用零件×2 | 无 | 木箱×1 |
| 石质斧镐 | 石料×3 + 木棍×1 + 绳索×1 | 无 | 石斧镐×1 |
| 简易铲子 | 木板×2 + 废金属×1 | 无 | 简易铲×1 |
| 短棍 | 木棍×2 | 无 | 短棍×1 |
| 木质长矛 | 木棍×1 + 废金属×1 + 绳索×1 | 无 | 木矛×1 |
| 基础火把 | 木棍×1 + 碎布料×1 | 无 | 火把×1 |
| 中级工作台 | 简易工作台×1 + 加厚木板×6 + 铁锭×2 + 通用零件×4 | 建造拆解Lv2 | 中级工作台×1 |

---

##### Phase 2: 合成 UI

###### 2.1 CraftingUI

**新文件**: `Assets/_Game/UI/CraftingUI.cs`
- 交互打开（E键点击工作台），全屏面板
- 左侧: 按 RecipeCategory 分页（食材/建材/武器/药品/弹药/改装）
- 右侧: 配方网格，显示图标+名称+材料清单
- 底部: 制作按钮 + 数量滑块（批量合成）
- 不可制作配方灰显 + 红字提示原因（缺材料/台子不够/技能不足）

###### 2.2 高级工作台 4 Tab UI
- Tab 1: 常规制作（同CraftingUI）
- Tab 2: 物资拆解 — 放入可拆解物品 → 产出预览 → 执行
- Tab 3: 装备维修 — 简易修理(随时) vs 精细满修(仅高级台)
- Tab 4: 装备改装 — 配件加装/拆卸/属性强化

###### 2.3 专属台 UI
- **篝火UI**: 食材投料区 → 自动计算饱食度 → 产出"杂烩菜"
- **熔炉UI**: 矿石+燃料 → 金属锭

---

##### Phase 3: 工作台放置与交互

###### 3.1 复用建造系统
工作台物品产出后是 ItemData（isWorkstation=true）→ 背包中选择放置 → 复用 BuildModeController 的 Ghost预览+放置流程 → 实例化 builtPrefab

###### 3.2 WorkstationObject
**新文件**: `Assets/_Game/Systems/Crafting/WorkstationObject.cs`
实现 IInteractable，E 键交互打开对应合成 UI，设置 CraftingSystem.ActiveStation。

---

##### Phase 4: 专属功能 + 食物组合 + 拆解维修

###### 4.1 食物组合烹饪
- 统一产物名"杂烩菜"
- 基础饱食度 = Σ所有食材饱食度（硬上限100）
- 技能加成：最终 = 基础 × (1 + 烹饪技能×系数)
- 溢出转 buff：超出部分 / 100 × 4小时
- 例：基础100 → 技能加成后150 → 溢出50 → 2h增益

###### 4.2 弹药通用基底
所有子弹共用火药+弹壳基底，换弹头区分类型。半成品定义：
- 火药 → 黑火药
- 弹壳 → 通用弹壳
- 手枪弹头 / 步枪弹头 / 霰弹 → 区分弹药类型

###### 4.3 拆解 + 维修 + 改装
- **拆解**：仅高级工作台，淘汰装备→基础材料回流
- **维修**：简易应急(随时上限低) + 精细满修(高级台，清debuff)
- **改装**：武器配件/护甲镶嵌，机械改装技能解锁

---

##### 物品清单速查

###### 原材料（野外直接获取）

| 大类 | 物品 |
|------|------|
| 植物 | 木材、草本纤维、食用野菜、野果、草药、毒草、棉料、杂草 |
| 水源 | 废水（污水）、生水（河水/雨水） |
| 矿物 | 石料、铁矿、铜矿、铅矿、煤炭、硫磺、硝石、水晶碎料 |
| 生物 | 生肉、鱼肉、兽皮、兽骨、羽毛、腐肉、变异组织 |
| 废料 | 碎布料、废金属、通用零件、空容器、橡胶原料、玻璃碎料、塑料碎片、废旧电线、电路板残件 |

###### 半成品

| 来源 | 物品 |
|------|------|
| 篝火 | 净水、熟肉、野菜餐、果饮、劣质熟腐肉 |
| 熔炉 | 铁锭、铜锭、铅锭、合金坯料、石砖、水泥原料、精炼煤炭 |
| 徒手 | 木棍、木板、绳索、粗布 |
| 弹药基底 | 通用弹壳、手枪弹头、步枪弹头、霰弹 |
| 化工 | 黑火药、基础油脂 |

###### 简易工作台产出

- 建材: 加厚木板、木质框架、木栅栏、小型储物木箱
- 工具: 石质斧镐、简易铲子、木质撬棍
- 生存: 简易纱布、基础火把、简易滤水器
- 武器: 木质长矛、短棍

###### 中级工作台产出

- 建材: 加固木构件、石质墙体、简易地基、门窗框架
- 工具: 铁制斧/镐/铲、钢制撬棍
- 防具: 皮甲套装
- 武器: 铁匕首、砍刀、铁矛、钢管
- 医疗: 止血绷带、基础草药膏、消毒棉
- 弹药: 普通木箭

###### 高级工作台产出

- **医疗**: 急救包、止血药剂、消炎药、止痛药、体力药剂、回血药剂、抗毒血清、全套医疗箱
- **弹药**: 手枪弹、步枪弹、霰弹、穿甲弹、燃烧弹、高爆弹、淬毒箭、弩箭
- **枪械**: 手枪、冲锋枪、突击步枪、狙击枪、霰弹枪、连发弩
- **护甲**: 铁质重甲、战术轻甲、军用重甲、防暴头盔、战术护具、防寒护甲、防辐射服
- **投掷**: 燃烧瓶、手雷、烟雾弹、闪光弹、诱饵道具
- **电力**: 电线、电池、电路主板、照明设备、简易发电组件、电控开关
- **载具**: 橡胶轮胎、传动零件、油箱、车架组件、制动配件
- **陷阱**: 捕兽夹、金属尖刺陷阱、电控警报机关

###### 拆解/维修/改装产物

- 拆解: 金属碎片、碎布料、武器零件、建筑废料
- 维修: 通用维修套件、精密修复材料、耐久恢复辅料
- 改装: 枪口配件、扩容弹夹、瞄准镜、武器配重、护甲贴片、减负内衬、防滑握把

###### 放置设施

篝火、烹饪炉灶、熔炼熔炉、简易工作台、中级工作台、高级工作台

###### 能源燃料

木柴燃料、精炼燃油、固态燃料、生物燃油

> **燃料用途区分**：煤炭（矿物，用于熔炉熔炼）≠ 木柴燃料（徒手/篝火燃料，由木材加工而来），两者是不同的物品。
> **空容器链条**：空容器（废料搜刮）→ 装水 → 净水/果饮成品；空容器 + 精炼燃油 → 燃油罐。装瓶配方消耗空容器，产出对应液体成品。
> **劣质熟腐肉**：腐肉在篝火烤制后的产物，可食用但有 debuff 风险，属于烹饪失败/低品质路线的消耗品。

###### 稀有特殊道具

各类装备蓝图、技能书籍、据点钥匙、稀有配方图纸

---

##### 文件清单

###### 新增

| 文件 | Phase |
|------|-------|
| `Config/ItemQuality.cs` | 1 |
| `Config/ItemUsageTag.cs` | 1 |
| `Config/WorkstationTier.cs` | 1 |
| `Config/RecipeData.cs` | 1 |
| `Config/RecipeCatalog.cs` | 1 |
| `Config/Editor/CreateRecipeCatalog.cs` | 1 |
| `Systems/Crafting/CraftingSystem.cs` | 1 |
| `Systems/Crafting/WorkstationObject.cs` | 3 |
| `Systems/Crafting/DeconstructSystem.cs` | 4 |
| `UI/CraftingUI.cs` | 2 |
| `Config/Recipes/**/*.asset` | 1~4 |
| `Config/Items/**/*.asset` | 1 |

###### 修改

| 文件 | 改动 |
|------|------|
| `Config/ItemCategory.cs` | 重写为 8 大类 |
| `Config/ItemData.cs` | 新增 quality / hasDurability / maxDurability / usageTags / isWorkstation / workstationTier |
| `Core/GameEvents.cs` | 新增 CraftingEvent |
| `Core/GameConstants.cs` | 确认 XP_CRAFT_ITEM / XP_CRAFT_WEAPON |

##### 验证

1. 编辑器 Create→Game→Recipe 创建SO，Inspector确认
2. CraftingSystem.CanCraft/Craft 验证三重检查+扣材料+产出+XP
3. 篝火UI验证饱食度叠加+上限100+溢出转buff
4. 端到端: 徒手做简易工作台→放置→E交互→制作物品
#### 4.3 电力/水源 ⬜
| 材料 | 说明 | 依赖 |
|------|------|------|
| 发电机 | 放置→加柴油→发电 | L3 建造 + 交互 |
| 电线 | 连接发电机和电器 | - |
| 总功率 | 电器耗电≤发电机输出 | - |
| 设备耗电 | 灯/冰箱/净水器 | - |
| 水管 | 抽水→过滤→储水→用水 | L3 建造 |

#### 4.4 区域/建筑 ⬜
| 材料 | 说明 | 依赖 |
|------|------|------|
| 区域类型 | 住宅/商业/工业/野外 | - |
| 建筑模板 | 预制体+容器标记 | L3 世界容器 |
| Loot 分布 | 按区域配置 | L3 世界容器 |
| 僵尸密度 | 按区域出生率 | L4 僵尸 AI |

#### 4.5 UI 内嵌清理 🔄（开发中）

> **设计原则**: UI 层与系统层彻底解耦。系统脚本不画 GUI，只通过 EventBus 发数据。所有 UI 集中在 `Assets/_Game/UI/` 管理。

##### 问题现状

系统脚本内嵌 OnGUI 渲染，互相重叠且无法统一管理：

| 内嵌位置 | 内容 | 问题 |
|----------|------|------|
| `TimeManager.cs:170` OnGUI | 天数+时间 | 与车速重叠 |
| `VehicleController.cs:329` OnGUI | 车速+油量+调试数据 | **(10,10) 重叠** |
| `PlayerCharacter.cs:171` OnGUI | 经验条 | ✅ 已删除 |

##### 解决方案：TopLeftHUD 上下文切换

```
现状:  TimeManager ──→ 自己画 (10,10)
       VehicleController ──→ 自己画 (10,10)  ← 互相覆盖

目标:  TimeManager ──→ EventBus ──→ TopLeftHUD.cs (Assets/_Game/UI/)
       VehicleController ──→ VehicleEntered/Exited ──→ TopLeftHUD.cs
                                                        ├─ 非驾驶 → 显示时间
                                                        └─ 驾驶中 → 显示车速/油量
```

##### 数据流

| 数据 | 来源 | 频率 | 方式 |
|------|------|------|------|
| 时间 | `TimeOfDayChanged` 事件 | 每帧 | 已有 |
| 天数 | `DayChanged` 事件 | 跨天 | 已有 |
| 驾驶状态 | `VehicleEnteredEvent` / `VehicleExitedEvent` | 上下车瞬间 | 已有 |
| 车速/油量 | `VehicleController` 公共属性 | Update 轮询 | 引用持有 |

> 车速不用事件——每帧发 struct 有 GC 压力。上车时 TopLeftHUD 拿到 VehicleController 引用，Update 直接读。

##### 改动清单

| 文件 | 动作 |
|------|------|
| **新建** `Assets/_Game/UI/TopLeftHUD.cs` | 上下文切换 + 统一左上角渲染 |
| `TimeManager.cs` | 删 `OnGUI()` |
| `VehicleController.cs` | 删 `OnGUI()` |

> **后期切换 Canvas**: 只改 TopLeftHUD 一个类，系统层完全无感。`OnGUI()` → `Canvas + Text`，接口不变。

---

## ⬜ 阶段 3：世界血肉（内容填充）

> 在阶段 2 骨架上填内容。所有逻辑框架已就绪，只需添加数据和配置。

| 模块 | 内容 |
|------|------|
| 物品扩展 | 100+ 物品（武器/食物/医疗/材料/杂物） |
| 僵尸类型 | 普通/快速/胖子/变异体 |
| 配方扩展 | 高级武器/防具/药品 |
| 地图扩展 | 多层建筑/地下室/秘密区域 |
| Loot 分布 | 按建筑类型配置 |
| 任务/事件 | NPC 求救/空投/尸潮 |
| 故事线 | 线索收集→结局 |
| 世界生成 | ✅ Phase1 一级模块（40m网格+多单元WFC+纯色可视化） |
| 职业扩展 | 进阶职业/技能树 |
| 车辆类型 | 轿车/皮卡/面包车 参数差异 |

## ⬜ 阶段 4：颜值（美术/UI/音效）

| 模块 | 内容 |
|------|------|
| 3D 模型 | 角色/僵尸/武器/建筑/家具 |
| 贴图/材质 | PBR 材质/地面/墙壁/物品 |
| 动画 | 行走/奔跑/攻击/僵尸动作 |
| UI | 摹客设计→Unity（图标/面板/边框） |
| 特效 | 攻击/受伤血花/爆炸 |
| 音效 | 脚步声/武器/僵尸/BGM |

---

## 🎯 设计哲学

### 深（写实模拟）
- **饥饿/食物**：多种食物+烹饪+变质腐败
- **健康/伤病**：出血→包扎、感染→抗生素、骨折→夹板
- **温度/衣物**：冬天穿厚衣、淋雨失温
- **人物成长**：属性+技能+职业差异化

### 简（精简处理）
- **电力**：发电机+柴油+电线=总功率，不铺电路
- **水源**：找到+煮沸=可饮用，不模拟过滤层级
- **建筑**：放墙体围栏，不算承重结构
- **载具**：找车+加油+修理，简化油耗

---

## 🏗️ 分工

| 工种 | 负责 |
|------|------|
| **代码（我）** | 阶段 1~2 所有系统逻辑，阶段 3 数据扩展代码，阶段 4 代码联动 |
| **美术（你/外部）** | 阶段 4 模型/UI/动画/音效 |
| **Unity 编辑器（你）** | 场景搭建、Asset 绑定、参数配置、UI 骨架拖拽 |

---

## 📊 当前进度

```
阶段 1 ████████████████████ 100%

阶段 2 L1 地基 ████████████████████ 100%

阶段 2 L2 框架 ███████████████████░  95%
  物品 ✅  背包 ✅  装备 ✅  交互 ✅  时间 ✅  人物 🔄  物品使用 ✅

阶段 2 L3 机制 ████████████████████ 100%
  世界容器 ✅ Phase2  生存数值 ✅ Phase1  战斗 ✅  建造 ✅  交通工具 ✅

阶段 2 L4 组装 ████████████████░░░░░░  82%
  ChunkManager ✅  ContainerRegistry ✅  UI内嵌清理 ✅  僵尸AI Phase1+2 ✅  声音系统 ✅ Phase1  背包拖拽 ✅  武器切换 ✅  角色面板 ✅  配方 📋  电力 ⬜  区域 ⬜
  └─ ✅ = 已完成  📋 = 前置计划已定稿  🔄 = 开发中

阶段 3 ██░░░░░░░░░░░░░░░░░░░░░░░░░░   8%
  世界生成 ✅ Phase1（40m网格+多单元WFC+纯色可视化）
阶段 4 ░░░░░░░░░░░░░░░░░░░░░░░░░░░░   0%
```

---

## 🏗️ 开发哲学：依赖优先，逐层添砖

> **一个系统无法独立完成。做到一半发现需要另一个系统做地基，就先去做那个地基。**

### 核心思想

系统之间存在**依赖链**，不可能跳过地基搭二楼：

```
你正在做 僵尸 AI
  └─ 需要 僵尸走路 → 先做 NavMesh 寻路（地基）
  └─ 需要 僵尸打人 → 先做 战斗系统（地基）
  └─ 需要 僵尸掉落 → 先做 世界容器（地基）
```

正确的做法：
1. 先做圆柱体（视觉）
2. 做寻路（地基）→ 僵尸会动了 ✓
3. **暂停**，去做战斗系统（下个地基）
4. 回来做僵尸打人
5. **暂停**，去做世界容器（下个地基）
6. 回来做僵尸掉落

### 进度追踪规则（更新版）

每个模块增加「所需地基」字段，直观看到依赖链：

```
模块名          Phase 1    所需地基（前置系统）
─────────────────────────────────────────────
僵尸 AI         ⬜         ➜ 寻路 + 战斗 + 世界容器
  寻路（NavMesh） ✅       独立（无依赖）
  战斗系统        ⬜        独立（依赖物品数据）
  世界容器        ⬜        ➜ 背包 + 交互
```

### 实际开发流程

```
当前系统 → 检查依赖 → 缺失依赖？
   ├─ 是 → 暂停当前系统，切去搭建依赖地基
   │        地基完成后 → 回到原系统继续
   └─ 否 → 继续开发
```

这样永远不会"卡住"，永远在做一个**可以独立完成**的子任务。

## 📋 模块完成度追踪（带依赖链）

> 每个模块遵循「基础实现 → 优化」两步走。基础实现完成即可用，优化自动排到后续阶段。
> 提出新模块时，自动按此格式记录到对应的阶段位置。

### 设计模式

| 字段 | 说明 |
|------|------|
| **模块名** | 系统名称 |
| **Phase 1（基础）** | 能用，功能完整，不做精致但不出 bug |
| **Phase 2（完整）** | 功能完备，加上第二阶段功能 |
| **优化项** | 自动排入后续阶段，注明「优化理由 + 计划阶段」 |

### 当前模块状态（带依赖链）

| 模块 | Phase 1 | 所需地基 | 优化项 |
|------|:-------:|---------|--------|
| **物品系统** | ✅ | 无（原始地基） | ① 运行时物品实例 → 阶段3<br>② 更多属性字段 → 阶段3 |
| **背包系统** | ✅ 拖拽旋转+选中+白框 | 物品系统（已完成） | ① 自动整理 → 阶段3<br>② 堆叠拆分 → 阶段3 |
| **装备系统** | ✅ Dictionary数据驱动 | 背包系统（已完成） | ① 装备模型可见 → 阶段4<br>② 更多装备槽 → 阶段3 |
| **物品使用系统** | ✅ | 背包系统（已完成） | - |
| **交互系统** | ✅ | 无（独立） | ① E检视/F拾取区分 → 阶段3<br>② 可交互高亮 → 阶段4 |
| **时间系统** | ✅ | 无（独立） | ① 天气系统 → 阶段4<br>② 季节系统 → 阶段4 |
| **人物系统** | ✅ Phase2 重构完成 | 无（独立） | 5大类10技能+全局经验池+体力+升级耗点梯度；SurvivalXPSystem/StaminaSystem/SkillCostTable/PlayerCombat重写/武器散射联动/Inventory负重联动/PlayerController体力联动/TimeManager时间经验 |
| **职业系统** | ✅ Phase1 完成 | 人物系统（已完成） | 8预设职业+1彩蛋(制作人)；CharacterTemplate初始装备/物品；ProfessionApplier三步投放；详见 2.7 |
| **地表物品** | ✅ 丢/捡/图标 | 背包+交互（已完成） | ① 同物品合并 → 阶段3<br>② 对象池 → 阶段3<br>③ 3D模型 → 阶段4 |
| **生存数值** | ✅ Phase1核心+HUD | 时间+人物（已完成） | ① 伤病系统 → 待排期<br>② 水源系统 → 待排期<br>③ 温度系统 → 待排期<br>④ 物品变质 → 阶段3 |
| **寻路（NavMesh）** | ⬜ | 无（独立地基） | → 僵尸AI 的起点 |
| **战斗系统** | ✅ Phase1~6全完成 | 物品数据（已完成） | ① 近战模型碰撞 → 阶段3<br>② 弹道线/枪口火焰 → 阶段4<br>③ 击退/硬直 → 阶段3<br>④ 耐久度 → 阶段3 |
| **世界容器** | ✅ Phase2完成 | 背包+交互（已完成） | ① 懒加载+ContainerRegistry → L4 ChunkManager<br>② 区块刷新 → L4 ChunkManager |
| **ChunkManager** | ✅ Phase1 | 世界容器+WorldGen网格（已完成） | 三级区块+预热队列+RefreshHub+性能分档+动态预热带+引导启动 |
| **ContainerRegistry** | ✅ Phase1 | ChunkManager+世界容器（已完成） | 两态追踪+懒加载+区块级刷新判定 |
| **僵尸 AI** | ✅ Phase1+2 | ChunkManager+寻路+战斗（全部就绪） | Phase1: FSM+NavMesh+集中感知+Chunk联动；Phase2: ZombieData SO+两阶段刷怪+视觉锥+Linecast+群体级联+预算追踪+3默认类型 |
| **声音系统** | ✅ Phase1 | ChunkManager（已完成） | DecibelSystem+SoundEmitter+3声源接入+空间分区+僵尸听觉联动；待接入: 建造/车辆/僵尸呻吟 |
| **建造系统** | ✅ Phase1 Ghost+放置+拆除+UI+7资产 | 交互+背包（已完成） | ① 加固系统 → 阶段3<br>② 更多建筑类型 → 阶段3 |
| **交通工具** | ✅ Phase1 驾驶+上下车+摄像机+油耗+SHIFT加速+防侧翻 | L1 物理+L2 交互（已完成） | ① 加油交互 → 阶段3<br>② 耐久度/撞损 → 阶段3<br>③ 更多车型 → 阶段3 |
| **配方/制作** | 📋 完整设计定稿 | 物品+工作站 | 4枚举+RecipeData SO+RecipeCatalog+CraftingSystem；22默认配方(徒手6+篝火4+熔炉3+简易台9)；详见 2.8 |
| **僵尸类型** | ⬜ | 僵尸AI（已完成） | -
| **角色面板** | ✅ Phase1 | 人物系统 | Tab页第2项：左(XP条+兑换+属性)+右(10技能分4组+升级)；手动XP兑换+FlashGreen动画 |
| **调试面板** | ✅ F2增强 | 人物系统 | F2/Y切换；XP/技能点交互控件；每技能↑升级按钮 |

---

## 🔧 Git 版本管理

> 仓库路径：`mygame1/`（Unity 项目根目录）

### 提交节奏

**每当完成一个 Phase 1（基础实现），立即提交一次。** AI 助手会在每个模块的基础实现完成后主动提醒提交。

### 提交信息规范

```
模块名(阶段): 简短说明

例：
物品系统(L2): Phase1 完成 — SO + 枚举 + 默认物品
背包系统(L2): Phase1 完成 — 多容器 + 负重 + UI骨架
战斗系统(L3): Phase1 完成 — 近战判定 + 伤害计算
```

### 当前仓库状态

| 项 | 值 |
|---|---|
| 分支 | `master` |
| 提交数 | 18 |
| 追踪文件 | ~780 |
| .gitignore | ✅ 已配置（排除 Library/Temp/Obj/UserSettings 等） |

---

## 📜 版本历史

> 每次提交记录在这里，方便追溯每次改动。

| 版本 | 日期 | 提交摘要 | 模块变化 |
|------|------|---------|---------|
| `v0.1` | 前期 | 初始提交: 生存游戏基础框架 | 阶段1 地基（移动/摄像机/碰撞/EventBus） |
| `v0.2` | 前期 | 初始化提交：阶段1+2框架完成 | L1 物品系统 + L2 背包/装备/交互/时间/人物 + 快捷栏/总面板UI骨架 + 拖拽系统 |
| `v0.3` | 2026-05-11 | UI(总面板): 物品覆盖层坐标定位修复 | BuildContainerGrid 新增物品名称/数量覆盖层（兄弟节点方案），全层 raycastTarget=false 修复拖拽，CreateItemNameOverlay anchor 偏移 bug 修复，数量始终显示 |
| `v0.3` | 2026-05-11 | 防御系统+物品使用交互+物品数据填充 | QuickItemBar 右键使用+中央进度条，防御系统(armorValue+TakeDamage)，9个消耗品+衣物数据 |
| `v0.3` | 2026-05-11 | 修复拖拽堆叠物品数据错乱 | RemoveItemAt坐标精确移除，同类型堆叠不误扣 |
| `v0.4` | 2026-05-11 | P0-P1 解耦重构 | QuickItemBar→EventBus，SurvivalSystem→事件驱动护甲 |
| `v0.4` | 2026-05-11 | 总面板ViewData重构(进行中) | InventoryViewData纯数据结构，逐步清理_inventory引用 |
| `v0.4` | 2026-05-11 | 替换摄像机预览为纯UI身体轮廓 | 删除Camera+RenderTexture, CreateBodyPreview纯UI绘制 |
| `v0.4` | 2026-05-11 | 添加头盔装备+Dictionary重构 | EquipSlot.Head, EquippedItems→Dictionary数据驱动 |
| `v0.5` | 2026-05-15 | WorldGen(阶段3): 40m基础单元+20×20多单元WFC | 8文件重构：GameConstants/ModuleData/WorldData/5个Stage重写，统一尺寸+Multi-cell坍缩+配额+锚点渲染 |
| `v0.5` | 2026-05-15 | 建造系统(L3): Phase1 完成 — Ghost预览+放置+拆除+BuildMenuUI+7默认资产 | BuildableData/GhostPreview/BuildModeController/PlacedStructure/BuildModeInputLock |
| `v0.5` | 2026-05-16 | 交通工具(L3): Phase1 完成 — 驾驶+上下车+摄像机+油量+后备箱+OffRoad模型 | VehicleData等8文件，Rigidbody+4 WheelCollider物理，IInteractable交互，事件解耦 |
| `v0.5` | 2026-05-18 | 交通工具(L3): SHIFT加速+防侧翻 — 两档速度+1.5倍油耗+反侧倾杆+重心+角速度阻尼 | VehicleData新增4字段，VehicleController添加AntiRoll+RollStabilization+Boost逻辑 |
| `v0.6` | 2026-05-19 | ChunkManager(L4): Phase1 完成 — 三级区块+预热队列+RefreshHub+容器两态+性能分档+引导启动+鼠标朝向+角色模型目录 | 10新建+5改造，100Chunk(10×10,800m×800m)，ContainerRegistry懒加载，TopLeftHUD上下文切换 |
| `v0.6` | 2026-05-20 | 僵尸AI(L4): Phase1 完成 — FSM五状态+NavMeshAgent+集中感知+Chunk联动+静态单例共享 | 8新建+4改造，5状态全局单例，ZombieAwarenessSystem集中检测，RuntimeChunk.zombieInstances联动激活 |
| `v0.6` | 2026-05-20 | 声音系统(L4): Phase1 完成 — DecibelSystem+SoundEmitter+三级节流+空间分区+3声源接入+僵尸听觉联动+噪声HUD+调试面板 | 7新建+10改造，三层架构，SoundSource/SoundTag两级标签，6级噪声梯度，Y键调试面板 |
| `v0.6` | 2026-05-20 | 僵尸AI(L4): Phase2 完成 — ZombieData SO+两阶段刷怪+视觉锥+Linecast遮挡+群体级联扩散+预算追踪+3默认类型+场景配置工具 | 6新建+6改造+4默认资产，两阶段预算制刷怪，隐蔽刷新点三重保障，逐帧级联扩散，DebugPanel联动 |
| `v0.6` | 2026-05-20 | 僵尸AI(L4): F1调试面板+圆柱体外型 — 距离/类型/数量可调配刷怪+ZombieData体型字段+CapsuleCollider缩放+ZombieController.Initialize修复Spawner数据失效 | 2新建+4改造，胖(0.5/2.0)/中(0.35/1.8)/小(0.25/1.6)三档体型，BuildBody静态工具方法 |
| `v0.7` | 2026-05-20 | 📋 人物系统(L2): Phase2 重构计划定稿 — 5大类10技能+全局生存经验池+体力系统+升级耗点梯度+10类行为获取经验 | 计划: 5新建+12改造，11步构建，4属性(移除智力)+10技能(战斗3/生存3/制造3/心智1)，100XP=1技能点 |
| `v0.7` | 2026-05-20 | 🔧 人物系统(L2): Phase2 重构实现 — 9个系统联动改造完成 | CharacterData(4属性+10技能枚举)/SkillCostTable(升级耗点表)/SurvivalXPSystem(全局XP池+技能点兑换)/StaminaSystem(体力消耗恢复+耐力联动)/PlayerCombat(近战伤害读新技能+体力检查)/WeaponShooting(枪械专精收窄散射锥)/Inventory(力量属性加成负重)/PlayerController(体力联动跑/走/减速)/TimeManager(每小时+每日时间经验+夜晚倍率)/BuildModeController(建造技能检查+XP奖励)/DamageableZombie(击杀XP)/DebugPanel(SurvivalXP+Stamina显示)/GameConstants(53个新常量) |
| `v0.7` | 2026-05-20 | 📋 职业系统(L2): Phase1 设计定稿 — 8预设职业+1彩蛋角色(制作人)+初始装备投放机制 | CharacterTemplate新增startingEquipment/startingItems；ProfessionApplier三步投放(穿装备→扩容→放物品)；9职业详细数值(属性修正+技能加成+装备+物品)；详见 2.7 |
| `v0.7` | 2026-05-20 | 🔧 职业系统(L2): Phase1 实现 — 9职业模板+初始装备物品投放 | CharacterTemplate加字段(2新字段)/ProfessionApplier(Equip→AddItem两步投放)/SurvivalXPSystem接入/CreateProfessionTemplates(一键生成9个.asset: 消警医工兵伐流技+制作人彩蛋)；Git: d1a5654 |
| `v0.7` | 2026-05-21 | 背包拖拽系统(L4): 旋转+选中+白框修复 | PlacedItem.rotated字段/InventoryContainer.IsSpaceFreeFor/DragDropManager坐标检测+T键旋转3上下文+选中白框直接overlay操作；Git: 5d0cdbf |
| `v0.7` | 2026-05-21 | 武器切换+腰带检测+装备去重(L4) | WeaponSwitcher(Q键循环4武器槽)/Inventory.EquipItem返回bool+targetSlot/HasBeltSpaceFor腰带空闲检测+belt_full事件；Git: 5d0cdbf |
| `v0.7` | 2026-05-21 | 角色面板(L4): Tab页第2项+手动XP兑换 | SurvivalXPSystem.AddXP仅累积+ConvertXPToPoints手动兑换/InventoryUI.ShowCharacterTabContent左面板(职业/XP条/属性)+右面板(10技能分4组+进度条+升级按钮)/FlashGreen闪绿动画/CharacterStatsChanged订阅自动刷新 |
| `v0.7` | 2026-05-21 | DebugPanel(L4): F2增强+交互控件 | F2键(Y键继续)切换面板/+50/+100/+500 XP按钮/兑换XP→点数/+1/+5技能点/每技能↑升级按钮 |
| `v0.7` | 2026-05-21 | Bug修复: FlashGreen协程销毁后访问 | FlashGreen改为onComplete回调模式，闪绿结束后才ShowOverview()，协程内Image null检查 |
| `v0.7` | 2026-05-21 | 📋 合成系统(L4): 完整设计定稿 | 4枚举+RecipeData SO+RecipeCatalog+CraftingSystem+文件夹重组；22个默认配方(徒手6+篝火4+熔炉3+简易台9)；物品清单速查(原材料4类+半成品4类+各工作台产出+能源燃料)；4 Phase实施计划；详见 2.8 |